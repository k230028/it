{
    "java.autobuild.enabled": true,
    "files.autoSave": "afterDelay",
    "files.autoSaveDelay": 1000,
    "java.compile.nullAnalysis.mode": "automatic",
    "java.configuration.updateBuildConfiguration": "automatic",
    // ─────────────────────────────────────────────────────────────────────────
    //  머신 종속 설정은 이 파일에 두지 않습니다 (REPO-01).
    //  `java.jdt.ls.vmargs`(lombok javaagent 절대경로)와 `java.import.gradle.home`
    //  (Gradle 설치 경로)은 PC마다 다르고, 워크스페이스 설정이 사용자 설정을 **덮으므로**
    //  여기 두면 다른 경로·다른 PC에서 Java 확장 임포트가 실패합니다.
    //  두 값은 각자의 **사용자 설정**(Ctrl+Shift+P → Preferences: Open User Settings (JSON))에
    //  넣습니다. 복사용 값과 배경 설명은 루트 `README.md`의 「IDE 설정(VS Code)」 참조.
    // ─────────────────────────────────────────────────────────────────────────
    // wrapper 배포판 자동 설치가 보안 프로그램에 막히는 환경이 있어 끕니다(README 참조).
    // 터미널의 ./gradlew는 wrapper를 그대로 쓰므로 영향이 없습니다.
    "java.import.gradle.wrapper.enabled": true,
    "files.encoding": "utf8",
    "files.autoGuessEncoding": false,
    "java.debug.settings.onBuildFailureProceed": true,
    // bin은 JDT(Buildship) 컴파일 출력 폴더입니다. 탐색기에서만 숨깁니다.
    // 주의: files.exclude는 UI·검색 필터일 뿐 클래스패스나 프로젝트 임포트에 영향이 없습니다.
    "files.exclude": {
        "**/bin": true
    },
    // 아래 목록은 확장 기본값 4개 + **/bin/** + **/.worktrees/**입니다.
    // 두 항목을 반드시 제외해야 하는 이유(중복 빈 등록·워크트리 프로젝트명 중복)는
    // 루트 README.md의 「IDE 설정(VS Code)」에 정리했습니다 — 지우면 기동이 실패합니다.
    "java.import.exclusions": [
        "**/node_modules/**",
        "**/.metadata/**",
        "**/archetype-resources/**",
        "**/META-INF/maven/**",
        "**/bin/**",
        "**/.worktrees/**"
    ],
    "editor.defaultFormatter": "redhat.java",
    "[java]": {
        "editor.defaultFormatter": "redhat.java", // 또는 google-java-format
        "editor.formatOnSave": true,
        "editor.tabSize": 4,
        "editor.insertSpaces": true
    },

    // ─────────────────────────────────────────────────────────────────────────
    //  Tailwind CSS / Nuxt 프론트엔드 설정 (it_frontend)
    //  - main.css의 @tailwind, @apply 지시어를 VS Code 내장 CSS LS가 모르므로
    //    내장 validator를 끄고 *.css 파일을 tailwindcss 언어 모드로 매핑합니다.
    //  - Tailwind CSS IntelliSense 확장(bradlc.vscode-tailwindcss)이 정확한 진단 제공.
    // ─────────────────────────────────────────────────────────────────────────
    "css.validate": false,
    "scss.validate": false,
    "less.validate": false,
    "css.lint.unknownAtRules": "ignore",
    "scss.lint.unknownAtRules": "ignore",
    "less.lint.unknownAtRules": "ignore",
    "files.associations": {
        "*.css": "tailwindcss"
    },
    "tailwindCSS.includeLanguages": {
        "vue": "html",
        "vue-html": "html"
    },
    "[vue]": {
        "editor.defaultFormatter": "Vue.volar"
    },
    "[javascript]": {
        "editor.defaultFormatter": "vscode.typescript-language-features"
    }
}
