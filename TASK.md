# IT정보화포탈 잔여과제

## 2026-09-12 문서 현행화(it-doc-sync)에서 등록한 항목

2026-09-03 08:06 이후 델타(백엔드 121·프론트 151·DB 26커밋)를 문서와 대조하면서 확인한 코드·저장소 결함이다. 문서 현행화 범위에서는 주석·문서만 고치고 비즈니스 로직은 바꾸지 않았으므로 아래는 모두 미조치 상태다. 각 항목은 현재 코드에서 재현 근거를 확인했다.

| ID     | 우선순위 | 상태    | 과제                                                                                                                                                                                                                                                                                                                              | 다음 조치                                                                                                                     | 근거 문서                                                                                                                                              |
| ------ | :------: | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SEC-24 |   높음   | ⬜ Open | `ServerMetricsController`(`/api/admin/dashboard/server-metrics`)가 `/api/admin/**` 컨트롤러 중 유일하게 클래스 수준 `@PreAuthorize("hasRole('ADMIN')")`가 없다. JavaDoc이 URL 규칙만 의존한다고 명시해 `SecurityConfig` 경로 규칙이 바뀌면 인증 사용자 전체에 열린다(`it_backend/CLAUDE.md` §5 관리자 API 규칙 위반) | 클래스에`@PreAuthorize` 추가, `ServerMetricsControllerTest`에 비관리자 403 케이스 추가                                    | `it_backend/src/main/java/com/kdb/it/common/admin/metrics/controller/ServerMetricsController.java`                                                   |
| BE-108 |   보통   | ⬜ Open | 전 직원용`GET /api/project-directory`의 `ProjectDirectoryService.findAll`이 `findAllByDelYn("N")`으로 최종본 사업을 **전체 엔티티**(CLOB 본문 4개 포함)로 읽고 projection·정렬·상한이 없다(`CLAUDE.md` §4 목록 규칙 위반). 사업 수에 비례해 응답과 메모리가 늘어난다                                             | 필요한 컬럼만 담는 Row 프로젝션과 명시적 정렬·상한으로 교체하고`*ProjectionIt` 동등성 테스트 추가                          | `it_backend/src/main/java/com/kdb/it/domain/budget/project/service/ProjectDirectoryService.java`, `ProjectDirectoryController.java`                |
| BE-109 |   낮음   | ⬜ Open | `ServerMetricsProperties` 컴팩트 생성자가 `sampleIntervalMs<=0`·`historyMinutes<=0`을 진단 없이 기본값(10초·60분)으로 치환한다. 루트 `CLAUDE.md` §4 "malformed 값을 기본값으로 숨기지 않는다" 위반으로 설정 오타가 조용히 동작한다                                                                                     | 0 이하·비정수 값은 기동 실패 또는 WARN 로그로 표면화하고, 미설정만 기본값 적용                                               | `it_backend/src/main/java/com/kdb/it/common/admin/metrics/config/ServerMetricsProperties.java`                                                       |
| BE-110 |   낮음   | ⬜ Open | `sso-verified` 쿠키 Max-Age가 `CookieUtil.SSO_VERIFIED_MAX_AGE=60`으로 고정돼 `jwt.sso-verified-validity`를 60초보다 길게 잡아도 쿠키가 먼저 사라진다. 문서·주석은 2026-09-12에 현행화했으나 설정 의도와 코드가 어긋난 상태다                                                                                              | 쿠키 수명을`JwtUtil`의 유효 시간에서 유도하거나 속성을 제거해 한 곳만 남김                                                  | `it_backend/src/main/java/com/kdb/it/common/util/CookieUtil.java`, `docs/guides/integrations/sso.md`                                               |
| BE-111 |   보통   | ⬜ Open | `ApplicationDashboardService.getDashboard`가 `RQS_DT`가 null인 대기 건을 `LocalDate.now()`로 간주해 항상 `normal`로 분류한다. 요청일 누락이 정상값으로 숨겨져 해당 건은 3일이 지나도 `urgent`가 되지 않는다(루트 `CLAUDE.md` §4 위반)                                                                                | null 요청일은 별도 상태(예:`unknown`)로 표면화하거나 진단 로그 후 제외                                                      | `it_backend/src/main/java/com/kdb/it/common/approval/service/ApplicationDashboardService.java`                                                       |
| BE-112 |   낮음   | ⬜ Open | `application.properties`의 `spring.task.scheduling.pool.size=3` 주석은 스케줄 작업이 셋이라고 적었지만 기본값 `app.mfa.store=jpa`에서 `MfaTransactionCleanupScheduler`가 네 번째 `@Scheduled`다. 네 작업이 동시에 지연되면 하나가 대기한다                                                                              | 풀 크기를 4로 올리고 주석에 조건부 스케줄러를 명시하거나, 등록 개수를 검증하는 테스트 추가                                    | `it_backend/src/main/resources/application.properties`, `it_backend/src/main/java/com/kdb/it/common/mfa/store/MfaTransactionCleanupScheduler.java` |
| FE-84  |   보통   | ⬜ Open | `useAssignableEmployee`가 작성자 소속 부서명 조회(`/api/users/{eno}`) 실패를 `console.error` 후 빈 문자열로 흡수한다. 사번 없는 부서 공용 담당자에게 부서명이 빈 값으로 채워져 저장될 수 있다(루트 `CLAUDE.md` §4 조회 실패≠빈 결과 위반)                                                                               | 실패를 오류 상태로 노출해 담당자 지정을 막거나 재시도 안내, 단위 테스트 추가                                                  | `it_frontend/app/composables/employee/useAssignableEmployee.ts`                                                                                      |
| FE-85  |   낮음   | ⬜ Open | `useCostConflictMerge.isNewLocalRow`가 숫자 타입 `Terminal.sno`를 `(sno as unknown as string) === ''`로 비교한다. 빈 문자열이 실제로 들어온다면 행 생성 지점의 타입이 잘못된 것이고, 아니면 죽은 분기다(`CLAUDE.md` §2 `as unknown as T` 금지)                                                                         | `addRow`가 넣는 값을 확인해 타입을 고치고 캐스트 제거                                                                       | `it_frontend/app/composables/cost/useCostConflictMerge.ts`                                                                                           |
| CQ-47  |   낮음   | ⬜ Open | `AuthService.isLoginBlocked`가 부점코드 국외 판정을 `bbrC.startsWith("9")`로 재구현하고 변수명을 `internalDept`로 두어 `BranchCodes.isForeign`("`9**`는 국외")과 의미가 반대로 읽힌다                                                                                                                                   | `BranchCodes.isForeign`으로 교체하고 변수명 정정, 기존 `AuthServiceTest` 유지                                             | `it_backend/src/main/java/com/kdb/it/common/system/service/AuthService.java`, `common/iam/BranchCodes.java`                                        |
| CQ-48  |   낮음   | ⬜ Open | 델타에`as unknown as T` 캐스트가 새로 늘었다: `useCostFormSave.ts`(날짜 정규화 3곳), `useCostPersistence.ts`, `useCostConflictMerge.ts`, `useResourceAmountFocus.ts`, `CostConflictMergeDialog.vue`. `CLAUDE.md` §2 규칙 범위("useFetch 어댑터·관리자 행 정제")를 넘어 확산 중이다                                | 제네릭·타입 가드로 대체하거나 불가피한 곳은 사유 주석과 함께 허용 목록화                                                     | `it_frontend/app/composables/cost/useCostFormSave.ts` 외                                                                                             |
| CQ-49  |   낮음   | ⬜ Open | `AppSidebar.vue`의 `adminLogMenuGroups` computed가 렌더링에 쓰이지 않는데 `eslint-disable-next-line no-unused-vars`로 보존돼 있다("그룹 분류 규약 보존" 주석). 죽은 코드가 규약 문서 역할을 대신하고 있다                                                                                                                   | 규약을`utils/menuPresentation.ts` 상수나 가이드로 옮기고 computed 삭제                                                      | `it_frontend/app/components/layout/AppSidebar.vue`                                                                                                   |
| CQ-50  |   낮음   | ⬜ Open | `it_backend/.compile-warn.log`(Gradle 출력 11줄)가 Git에 추적된다. `98cd32a5`가 `.javadoc.log`·`bootrun-codegen*.log`·`test-output.txt`는 제거했으나 이 파일은 남았다                                                                                                                                                 | `git rm --cached` 후 `.gitignore`에 `*.log` 추가                                                                        | `it_backend/.compile-warn.log`                                                                                                                       |
| CQ-51  |   낮음   | ⬜ Open | `AdminService`의 1인자 `toRoleResponse(CroleI)`(사번마다 `resolveUserName`을 따로 조회하는 N+1형 변환기)는 호출자가 없는 죽은 코드다. 2026-09-12 테스트 유지보수에서 JaCoCo가 해당 줄(288~299)을 전량 미커버로 보고했고, 테스트로 도달할 수 없어 보강 대상에서 제외했다                                                     | 오버로드 삭제(2인자`toRoleResponse(CroleI, Map)`만 유지), 필요하면 `resolveUserName(String)` 단건 조회 사용처도 함께 점검 | `it_backend/src/main/java/com/kdb/it/common/admin/service/AdminService.java`                                                                         |

## 2026-09-10 이전 상태

FE-79~83·BE-106(2026-09-10 첨부 기능 후속)과 BE-101~105·FE-74~78·CQ-45·46(2026-09-08 동시성 병합 후속)은 모두 종료되어 2026-09-12에 [`TASK_DONE.md`](TASK_DONE.md)로 옮겼다. FE-82·FE-83은 조치 코드가 같은 날 기능 철회로 삭제되어 완료가 아닌 폐기로 기록했다. SEC-21~23, BE-90~100, FE-68~73, CQ-42~44도 같은 파일에 있다.

`V20260903_004`·`V20260903_005`가 목표로 한 접두어별 BGDOC 인덱스는 `V20260907_002`의 통합 인덱스로 대체됐다. 운영 DB에 남은 인덱스 교체와 데이터 보정 항목은 [`meta/backlog.md`](meta/backlog.md)가 관리하며, 운영 적용·재추출 전에는 실제 인덱스 현황인 `meta/index.txt`를 수정하지 않는다.
