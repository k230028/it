# IT Portal 잔여과제

## 2026-09-03 현재 상태

구현·검증이 끝난 과제는 이 파일에 남기지 않고 [`TASK_DONE.md`](TASK_DONE.md)에서 관리합니다.

| ID | 우선순위 | 상태 | 과제 | 다음 조치 | 근거 문서 |
| -- | :------: | ---- | ---- | --------- | --------- |
| SEC-21 | 높음 | ⬜ Open | 정보화사업 목록·bulk 조회가 부서 범위 검사를 우회한다. `ProjectController.getProjects`·`bulkGetProjects`가 인증 주체를 받지 않고, `ProjectRepositoryImpl`은 클라이언트가 `svnDpmC`를 보냈을 때만 부서 조건을 건다. 상세(`getProject`)는 `BudgetDetailAccessVerifier`로 403인데 bulk는 같은 `ProjectDto.Response`를 200으로 돌려준다 | 두 엔드포인트에 `@AuthenticationPrincipal`을 받아 관리자·IT조직이 아니면 부서 조건을 인증 주체 값으로 덮어쓰고, bulk는 범위 밖 항목을 `failedIds`로 분리. `countProjectList`도 같은 범위 공유 | `it_backend/docs/guides/security/data-scope.md` |
| SEC-22 | 높음 | ⬜ Open | 전산업무비 bulk 조회에만 부서 범위 검사가 없다. 목록(`CostService.searchCostList`)과 상세(`verifyDeptReadable`)는 보호되는데 `getCostsByIds(request)`는 인증 주체를 받지 않는다 | `getCostsByIds(request, user)`로 시그니처를 바꾸고 조립 결과의 `costSvnDpmC`마다 `verifyDeptReadable` 적용. SEC-21과 같은 묶음으로 회귀 테스트 추가 | `it_backend/docs/guides/security/data-scope.md` |
| SEC-23 | 보통 | ⬜ Open | Q&A 게시판 목록이 비공개 문의의 제목·작성자를 전 사용자에게 노출한다. `BoardPostService`가 Q&A 게시판에 `includePrivatePosts=true`를 무조건 넘기고 서버가 강제하는 부서 조건이 없어, 투영의 `nacNm`·`fstEnrUsid`·`usrNm`·`bbrNm`이 그대로 나간다(본문 상세만 403) | 자물쇠 표시가 목적이므로 타 부서 비공개 행은 제목·작성자를 마스킹하거나 술어를 `xpoYn='Y' OR bbrC = :actorBbrC`로 좁힌다. `BoardPostServiceTest`의 현재 동작 고정 테스트도 함께 갱신 | `it_backend/.../BoardPostRepositoryImpl.java:143` |
| BE-90 | 높음 | ⬜ Open | `BGDOCM` 문서번호 접두사 `GDOC-` 네임스페이스 오염. 공통 안내 팝업(`common.popup`)과 스피드다이얼 담당자 정보(`SPEED_DIAL_CONTACT_INFO`)가 사용자 가이드와 같은 `GDOC-` 접두사를 쓰는데, `GuideDocService.getDocumentList()`는 접두사만으로 필터해 두 문서를 가이드 문서로 노출하고 관리자 화면에서 수정·삭제까지 허용한다. 로컬 DB 실측으로 `GDOC-2026-0461`(SPEED_DIAL_CONTACT_INFO) 확인 | 신규 재사용 문서에 전용 접두사를 부여하거나, `GuideDocService`의 목록·단건·수정·삭제 술어에 `DOC_TTL_CONE` 제외 조건을 추가. 기존 행은 마이그레이션으로 개번 | `it_backend/.../GuideDocService.java:43` |
| BE-91 | 보통 | ⬜ Open | 금융정보단말기 일괄 반입의 기준연도가 `"2025"`/`"2026"` 문자열로 하드코딩되어 있다. DTO 필드명도 `costId2025`/`costId2026`이라 API 계약에 연도가 박혀 있어 2027년에는 잘못된 연도 원장에 적재된다 | 기준연도를 요청 파라미터나 예산기간 설정에서 받도록 바꾸고 DTO 필드를 연도 중립 이름으로 전환 | `it_backend/.../TerminalBulkImportPlanner.java:24` |
| BE-92 | 보통 | ⬜ Open | OpenAPI `@Operation(description)`의 컬럼 한글명 오기. `APG_FL_KD_NM`·`APG_FL_LNK_CTZ_NM`을 `주식별자컬럼명`·`주식별자내용`으로 서술하는데, `meta/meta.txt` 기준 실제 이름은 `첨부파일종류명`·`첨부파일연결콘텐츠명`이고 `주식별자컬럼명`은 전혀 다른 컬럼 `PK_COL_NM`이다. `app/types/api.d.ts`에 10곳 전파됨 | 배너·파일·사용자가이드 컨트롤러의 description을 정정한 뒤 백엔드를 기동해 `npm run codegen` 재생성, `npm run codegen:check` 통과 확인 | `meta/meta.txt:66574,66583,62155` |
| BE-93 | 보통 | ⬜ Open | 조직명 스냅샷의 UTF-8 바이트 검증이 `Btermm`에만 있다. `Bcostm.assignSvnOrgNames`와 `Bprojm`의 `SVN_DPM_NM`·`SVN_TEM_NM`·`TLR_NM`·`USR_NM`·`BZ_DTT_NM`(각 100 BYTE)은 `@Column(length)`만 있어 한글 34자 이상에서 ORA-12899로 실패한다 | `Utf8ByteLimit` 검증을 같은 계열 컬럼 전반으로 확대 | `it_backend/.../Btermm.java:266` |
| BE-94 | 보통 | ⬜ Open | 전산업무비 이력 조회가 개정본마다 상세를 재조회한다(1+N). 대칭 구현인 `ProjectController.getHistory`는 `findHistory` 한 번으로 끝낸다. 또한 `CostVersionService`에는 actor 오버로드가 없어 권한 경계가 Controller에 남아 있다 | `CostVersionService`에 `findHistory(costBgNo, actor)`·`createReapplication(costBgNo, actor)` 오버로드를 추가하고 이력 응답을 배치 조립으로 전환 | `it_backend/.../CostController.java:113` |
| BE-95 | 높음 | ⬜ Open | `V20260901_002__AddReapplicationDraftUniqueness.sql`이 추가(`6954def`) 후 삭제(`1e563c5`)됐고 두 커밋 모두 `main`에 있다. 중간 상태를 당겨 적용한 환경은 `flyway_schema_history`에 `20260901.002` 행이 남은 채 스크립트가 없어 기동이 막힌다. `docs/operations/`에 인계 기록이 없다 | `it_database/docs/operations/2026-09-01-draft-uniqueness-withdrawal.md` 작성(이력 행 정리 또는 `flyway repair` 절차, 인덱스 잔존 확인법)하고 버전 `20260901_002`는 재사용 금지로 봉인 | `it_database` `6954def`·`1e563c5` |
| BE-96 | 높음 | ⬜ Open | `V20260831_003__RevertCharSemanticsToByteSemantics.sql`이 환경 게이트 없이 업무 문자열을 자동 절단한다. 주석은 "로컬의 잘못된 초과 값만"이라 하지만 dev/prod 수동 적용에서도 같은 UPDATE가 돈다. 실제 절단 발생 기록이 `V20260831_004` 주석에 남아 있고 `docs/operations/`에 승인·백업·복구 기록이 없다 | 절단 대신 사전 점검 결과로 중단시키는 후속 마이그레이션을 추가하고, 절단이 필요하면 별도 승인 마이그레이션으로 분리. 사후 인계 기록 작성 | `it_database/CLAUDE.md` §2 |
| BE-97 | 보통 | ⬜ Open | 가이드 콘텐츠 이관 계획이 예약한 `V20260901_001__AddBusinessGuideTitleUniqueness.sql`이 존재하지 않고, 그 버전번호는 이미 `RestoreCapplaReapplicationIndex`가 소비했다. 계획을 문자 그대로 실행하면 Flyway duplicate version으로 기동이 막힌다 | 계획서의 버전을 미사용 번호로 개번한 뒤 산출물 작성. BE-90(GDOC 네임스페이스)과 함께 검토 | `docs/superpowers/plans/2026-09-01-guide-content-migration.md:38` |
| BE-98 | 보통 | ⬜ Open | 파괴적 DDL 3건에 운영 인계 기록이 없다 — `V20260829_001`(테이블 교체 + `DROP TABLE CASCADE CONSTRAINTS` 3회), `V20260831_003`(950여 컬럼 `ALTER MODIFY`), `V20260831_005`(`DROP COLUMN` 4회, PK 재생성 2회). `V20260831_005`는 존재 확인 가드도 없어 재실행 시 실패한다 | `it_database/docs/operations/`에 소급 기록 작성. 재실행 가드 필요 여부 검토 | `it_database/docs/guides/migrations.md` |
| BE-99 | 보통 | ⬜ Open | `V20260829_001`이 자기 헤더의 `VARCHAR2(100 BYTE)` 약속과 달리 bare `VARCHAR2(100)`으로 생성하고 `CHAR_USED` 검증 블록도 없다. 현재 `'B'`인 것은 이틀 뒤 `V20260831_003`이 스키마 전체를 훑은 결과일 뿐이다. 관련해 `2026-08-29-terminal-org-name-byte-semantics.md`는 `DATA_LENGTH = 100` 기대치를 하드코딩해 우연히 맞는 상태를 정상으로 확정한다 | 신규 `CREATE TABLE` 마이그레이션에 `CHAR_USED`·`DATA_LENGTH` 검증 블록을 의무화하고, 인계 문서는 하드코딩 대신 그 블록을 참조하도록 정정 | 루트 `CLAUDE.md` §3 |
| BE-100 | 낮음 | ⬜ Open | 잔존물·중복 정리 — 철회된 `V20260901_002`를 예정 산출물처럼 안내하는 stale 참조 2곳, 미사용 `Cinfmm.markDispatched` 위임 래퍼, 문서번호 채번 4중복(`CommonPopupCreationService`·`ContactInfoCreationService`·`GuideDocService`·`FormGuideService`), 구 경로 `Utf8ByteLimitTest.java` 중복 | 참조 정리, 미사용 메서드·구 테스트 삭제, 채번을 공통 헬퍼로 추출 | `it_database/docs/operations/2026-09-01-flyway-002-checksum-recovery.md:65` |
| FE-68 | 보통 | ⬜ Open | 게시판 목록의 첨부파일 일괄 조회 실패를 "첨부 없음"으로 위장한다. 대상은 이미 첨부가 있다고 판정된 행인데, 실패 시 `attachmentsByPost = {}`가 되어 전부 `-`로 그려진다. 같은 결함을 `useInfoHomeFeed`는 실패 플래그와 배너로 이미 표면화한다 | `useInfoHomeFeed`와 같은 형태로 실패 플래그를 두고 컬럼에 재시도 가능한 실패 표식을 렌더 | `app/pages/board/[blbMngNo]/index.vue:79` |
| FE-69 | 보통 | ⬜ Open | 스피드다이얼 담당자 정보 조회 실패가 "미작성"과 같은 상태로 취급된다. `contactInfo`가 실패 시 `{docMngNo:null, contentHtml:null}`로 떨어지고 버튼 노출 조건이 그 값을 보므로 항목이 사라진다. 다이얼로그의 오류·재시도 UI가 도달 불가 | 노출 조건에 `contactInfoError`를 포함해 실패 시에도 항목을 남기고, 본문이 비어 있는 정상 응답일 때만 감춘다 | `app/components/layout/SpeedDial.vue:112` |
| FE-70 | 보통 | ⬜ Open | 예산·정보화사업 보고서의 `sessionStorage` 파싱 실패 처리 결함 2건. (1) 정보화사업 ID만 손상되면 "둘 다 비었을 때" 경고에 걸리지 않아 선택 사업이 빠진 보고서가 정상 출력된다. (2) `removeItem`이 `JSON.parse` 뒤 try 블록 안에 있어 손상 항목이 지워지지 않고 재방문마다 같은 실패를 반복한다 | 파싱 실패를 "선택 없음"과 분리해 별도 안내를 내고 `removeItem`을 `finally`로 이동 | `app/pages/budget/report.vue:248` |
| FE-71 | 낮음 | ⬜ Open | 델타 신규 파일 중 `useUserGuide.ts`만 생성 스키마(`UserGuideDto.Response`)를 두고 수기 타입 `UserGuideRecord`를 선언했다. 형제 신규 파일은 모두 생성 스키마 별칭을 쓴다. 필수·nullable도 어긋나 필드 삭제 드리프트를 컴파일러가 못 잡는다 | 생성 스키마 별칭으로 전환 | `app/composables/useUserGuide.ts:26` |
| FE-72 | 낮음 | ⬜ Open | 미사용 export·중복 구현 정리 — `BYTE_LIMITS`·`ProjectTextLimit`(참조 0건), `escapeHtml` 완전 동일 중복 2곳, `stripHtml` 재구현 2곳(엔티티 디코딩·줄바꿈 변환 차이가 있어 단순 치환 불가), 리치에디터 본문 유의미성 판정 3중 복제 | 공통 유틸로 승격 후 중복 제거, 미사용 export 삭제 | `app/utils/projectFormLimits.ts:22` |
| FE-73 | 낮음 | ⬜ Open | 게시판 멘션 버튼의 `aria-label`이 한국어로 하드코딩되어 있다. 같은 문구가 `i18n/messages/layout.ts`의 `employeeInfo` 키로 이미 있고 `AppHeader.vue`는 그 키를 쓴다. 순수 유틸이라 i18n 컨텍스트가 없는 것이 원인 | 호출부에서 번역 함수를 넘기도록 시그니처 확장 | `app/utils/boardContent.ts:37` |
| CQ-42 | 높음 | ⬜ Open | `npm test`가 max-lines ratchet에서 실패한다. 예외 맵(`OVERSIZED_FILE_BASELINES`)이 비어 있는데 델타에서 7개 파일이 800줄 상한을 넘겼다 — `TiptapToolbar.vue`(948), `info/projects/[id].vue`(906), `info/cost/[id].vue`(892), `info/index.vue`(878), `TiptapEditor.vue`(843), `info/projects/form.vue`(809), `info/cost/form.vue`(801) | 7개 파일 책임 분리. 기준선 예외로 무마하지 않는다 | `it_frontend/CLAUDE.md` §1 |
| CQ-43 | 높음 | ⬜ Open | `npm run check`가 ESLint 에러로 실패한다. `renderTiptapMentions`의 정규식에서 수량자 `[^>]*` 뒤의 단어 경계 단언이 최소 0회와 모순돼 `regexp/no-contradiction-with-assertion` 위반 | 첫 수량자를 1회 이상으로 바꾸거나 단어 경계 대신 공백 매칭을 명시 | `app/utils/boardContent.ts:28` |
| CQ-44 | 높음 | ⬜ Open | `npm run typecheck`이 main에서 2건 실패한다. 전산예산 신청서 PDF의 `{table, layout}` 객체가 pdfmake `Content` 타입에 배정되지 않는다(`d4028dc7`, 2026-09-02 유입). CQ-43과 함께 `npm run check` 전체를 red로 만든다 | `CustomTableLayout` 분기를 포함하도록 인자 타입을 좁히거나 pdfmake 타입 정의에 맞게 조립부를 정리 | `app/features/approval/forms/itBudget/pdf/projectSection.ts:37` |

세부 구현 내용, 검증 명령과 결과, 운영 인계 조건은 [`TASK_DONE.md`](TASK_DONE.md) 및 각 근거 문서에 기록합니다. `TEST.md`는 실행 결과 원문으로 보존하며 수정하지 않습니다.
