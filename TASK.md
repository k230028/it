# IT Portal 잔여과제

|  우선순위   | 의미           |
| :---------: | -------------- |
| 🔴 Critical | 즉시 조치      |
|   🟠 High   | 조속 조치      |
|  🟡 Medium  | 계획 반영      |
|   🟢 Low    | 선택 개선      |
| 🏛️ External | 외부/운영 의존 |
|   ✅ Done   | 확인 완료      |

## 🔒 보안

| ID     |     우선순위     | 유형 | 과제                                  | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | :--------------: | ---- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-12 |     🟢 Low      | MFA  | OnePass FIDO 사용자 거부 상태 구분    | `trResultConfirm`이 `resultCode=100000`이면서 `trStatus != 1`인 응답을 모두 미결정(UNDECIDED)으로 본다. 연동 규격에 사용자 거부를 뜻하는 `trStatus` 값이 정의되어 있지 않아 거부와 대기를 구분하지 못하며, 거부한 거래도 challenge 만료(90초)까지 미결정으로 남는다. 규격에 거부 상태값이 추가되면 `OnePassClient.confirmFido`의 분기에서 `failure()`로 분리한다.                                                     |
| SEC-13 |     🟢 Low      | MFA  | MFA 거래의 다중 인스턴스 지원         | 로그인 대기·MFA 거래를 애플리케이션 메모리에만 두어 서버 재시작 시 진행 중 거래가 모두 폐기되고, 다중 인스턴스 배포를 지원하지 않는다(설계 §8에서 이번 범위 제외). 필요해지면 `MfaTransactionStore`·`LoginPendingTransactionStore` 인터페이스를 공유 캐시 구현으로 교체한다.                                                                                                                                        |
| SEC-14 | 🟡 Medium | 파일 | 업로드 저장경로의 `pkColNm` 정화 | `FileUploadUnitService.buildStorageDir`가 클라이언트 `pkColNm`을 그대로 `Paths.get(basePath, pkColNm, 년, 월)`에 넣어 `../` 세그먼트로 basePath 밖에 디렉터리·파일을 만들 수 있다(다운로드는 `startsWith(base)` 검증으로 차단되어 쓰기 측만). 확장자 화이트리스트·서버 채번 파일명이라 임의 코드 배치는 제한되나 `file-security.md`의 경로 정규화 원칙과 어긋난다. 정규화 후 basePath 내부인지 검증하거나 `pkColNm`을 허용 문자 집합으로 제한하고, 미등록 종류의 업로드 허용 정책(쓰기 registry의 레거시 보존)도 함께 재검토한다. 2026-08-19 REVIEW 보안 검토. |
| SEC-15 | 🟢 Low | 파일 | 반입 원본 읽기 판정의 논리 삭제 미필터 | `RequestFormFileReadAuthorizer`가 부르는 `ApplicationMapRepository.findByApfDcmNo`와 원장 `findById`가 `DEL_YN`을 거르지 않아, 매핑·원장이 논리 삭제되거나 재매핑돼도 구 부서 사용자가 반입 원본을 계속 열람할 수 있다. 현재 코드베이스에 `Cappla` 논리 삭제 호출부가 없어 잠재 결함이며 즉시 악용은 불가. `findByApfDcmNoAndDelYn`과 활성 원장 조회로 교체한다. 2026-08-19 REVIEW 보안 검토. |

_SEC-04·SEC-05 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-19 Remediation Phase 1, SEC-06은 같은 문서의 2026-07-19 배포 정합성 후속, SEC-08·SEC-09는 2026-07-25 보안 트랜잭션 무결성 절을 참조합니다._

_SEC-10(지정맥 BioAgent 연동 규격 반영)과 SEC-11(지정맥 해시 검증 도입)은 `mfa.md` 반입 직후 같은 작업에서 해소해 표에 남기지 않습니다. 규격과 구현 대조 결과는 `it_frontend/README.md`의 「추가 인증(MFA) 연결」을 참조합니다._

## 🤝 사전협의

> 현재 활성 사전협의 과제 없음. 2026-07-26 완료·폐기 항목은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## ⚠️ 에러 처리

> 현재 활성 에러 처리 과제 없음.

_ERR-09·ERR-10 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-26 에러 표면화·복구 기록을, ERR-06은 2026-07-19 Remediation Phase 1, ERR-05는 같은 문서의 2026-07-19 배포 정합성 후속 기록을 참조합니다. ERR-11·ERR-12는 같은 문서의 2026-07-29 저장 복구·조회 실패 표면화 기록을, ERR-13은 2026-07-31 ERR-13·FE-15~19 조치 기록을, ERR-14는 2026-08-06 잔여과제 저비용 배치 1 기록을 참조합니다._

## 🎨 프론트엔드

| ID    |  우선순위  | 유형 | 과제                                            | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | :--------: | ---- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FE-47 | 🟡 Medium | 테스트 | 정보 홈·보고서 E2E 6건 실패 원인 규명 | 2026-08-19 TEST.md 실행에서 `info-home.spec.ts` 3건과 `report-pdf-latest.spec.ts` 3건이 부하와 무관하게 재현 실패한다(BOARD-1 해소 후 전체 재실행에서도 동일)(단독 실행·전체 실행·서버 재기동 후 모두 동일). `board.spec.ts` 7건과 달리 BOARD-1 i18n 결함(2026-08-19 해소)이 원인이 아니어서 별건이다. 증상 — ① 정보 홈: `공지 다시 시도` 버튼 클릭 중 `element was detached from the DOM, retrying`가 반복되다 60초 타임아웃, 최종 스냅샷은 로그인 화면이라 도중에 로그아웃이 일어난다. 실제 백엔드(28080)가 기동 중이면 mock되지 않은 호출이 네트워크 오류가 아니라 **401**로 돌아와 로그아웃 경로를 타는 것이 유력한 가설이므로, 백엔드를 내린 상태와 올린 상태를 나눠 재현해 갈라야 한다. ② 보고서: `팀장 결재자 지정/변경` 버튼을 찾지 못해 90초 타임아웃인데 페이지 자체는 정상 렌더링된다 — 버튼 라벨·노출 조건이 바뀌었는지 확인이 필요하다. 두 건 모두 테스트 수정으로 끝날 수도, 제품 회귀일 수도 있어 원인 규명이 선행돼야 한다. |
| FE-48 | 🟢 Low | 테스트 | 알림 헤더 E2E가 부하에 따라 간헐 실패 | `notifications.spec.ts`의 4건 중 1~3건이 실행마다 다르게 실패한다 — 재시도에서 통과하는 실행도 있고 재시도까지 실패하는 실행도 있다(2026-08-19 실측: 전체 실행 시 3건 실패 → 단독 실행 시 1건 → 4개 spec 묶음에서는 flaky 1건). 실패 지점이 `page.reload()` 직후 `getByRole('button', { name: '알림' })` 대기와 드롭다운 열림 대기로, `playwright.config.ts` 주석이 설명하는 Nuxt dev 지연 컴파일 비용에 걸리는 부류다. 고정 timeout을 늘리기보다 뱃지·드롭다운이 데이터 도착 후 안정화되는 시점을 기다리도록 단언을 바꾸는 편이 낫다. |
| FE-46 | 🟢 Low | 테스트 | `useBudgetStatusColumns`의 도달 불가 분기 정리 | `app/composables/budget/useBudgetStatusColumns.ts`의 `localize`가 `group: groupKey ? t(groupKey) : undefined`로 삼항 분기를 갖는데, `ColumnSpec.groupKey`가 옵셔널(`groupKey?: string`)일 뿐 실제로는 **모든 spec이 groupKey를 갖는다** — 직접 선언 17건 전부와 `amountColumns`·`assetDetailColumns` 헬퍼 4회 전개가 모두 `groupKey`를 필수 인자로 넘긴다. 그래서 `: undefined` 쪽은 테스트로 진입할 통로가 없는 죽은 분기이고, 이 파일의 전체 분기가 2개뿐이라 branches가 **50%에서 고정**된다(TEST.md 70% 기준 미달의 유일한 원인). 소스 수정 금지 원칙으로 2026-08-19 TEST.md 실행에서는 손대지 않았다. 해소는 둘 중 하나 — ① `groupKey`를 필수로 바꾸고 삼항 제거, ② `localize`나 spec 배열을 export해 그룹 없는 spec을 테스트가 주입. functions는 같은 실행에서 87.5% → 100%로 올렸다(`otherGroupLabel` 미평가 해소). |
| FE-49 | 🟡 Medium | i18n | `RequestFormResultTable`의 잔존 하드코딩 라벨과 감사기 Record 사각지대 | `statusLabel`(`'반영'`·`'차단'`·`'실패'`·`'대상 아님'`)·`statusText`(`'결정 완료'`)·`fieldLabel`(`curC: '통화'` 등)이 하드코딩 한국어로 템플릿에 렌더되는데, `check-user-facing-copy`가 Record 값 리터럴을 탐지하지 못해 이 파일이 exit 0으로 통과한다 — 기준선 0건(FE-37)은 이 사각지대를 포함한 착시다. 두 Record를 `migration.bulkImport.result.*` 키로 이관하고, 감사기의 Record 값 탐지 보강을 검토한다. 같은 계열 사례: `RealtimeFeedTable.vue`의 `chgLabel`(switch-case return 리터럴 '생성'/'수정'/'삭제')도 감사기를 통과한 채 렌더링된다 — i18n판 `realtimeLogs.chgTypeLabel`이 이미 있으므로 교체만 하면 된다. 2026-08-19 REVIEW. |
| FE-50 | 🟢 Low | 정리 | `types/mfa.ts`의 `MFA_METHOD_LABEL` 죽은 export | `MfaDialog`가 `t('auth.mfa.*')` 키로 라벨을 만들면서 소비처가 0곳이 됐다(app·tests 전수 grep 확인). 상수를 삭제한다. 함께 정리: 같은 파일의 `qrImageSource`도 소비처 0곳(MfaDialog가 QrcodeVue로 직접 렌더링, 델타 밖 커밋에서 마지막 소비처 제거)이고, `i18n/messages/common.ts`의 `common.messages.warning` 키(ko·en)도 정적·동적 참조 0건인 고아다. `MFA_METHOD_ICON`·`MFA_ERROR_MESSAGE`는 사용 중이라 대상이 아니다(BIO_AGENT_FINGER_TYPES의 labelKey는 FE-51 참조). 2026-08-19 REVIEW. |
| FE-51 | 🟢 Low | MFA | 지정맥 손가락 선택 UI 미배선 | `useMfa.selectFingerType`은 export만 되고 호출하는 컴포넌트가 0곳이라 지정맥 인증이 항상 `DEFAULT_BIO_AGENT_FINGER_TYPE('10', 오른손 검지)`로 나간다 — 다른 손가락을 등록한 사용자는 인증에 실패할 수 있다. `BIO_AGENT_FINGER_TYPES`의 `labelKey`와 `common.fingerTypes.*` 12개 i18n 키(FE-37에서 이관)도 그리는 화면이 없어 고아다. MfaDialog에 손가락 선택 UI를 배선하거나, 기본값으로 충분하다고 판단되면 `labelKey`·i18n 키·`selectFingerType` export를 정리한다 — 업무 판단 필요. 2026-08-19 REVIEW. |
| FE-52 | 🟡 Medium | 타입 | `main`의 기존 typecheck 회귀 3건 | `npm run typecheck`가 `app/composables/migration/useRequestFormPage.ts:66`(TS2322)과 `tests/unit/composables/migration/useRequestFormPage.test.ts`·`useRequestFormUpload.test.ts`(TS2345) 3곳에서 `archiveOnly` 속성 누락으로 실패한다. 이관(migration) 작업이 커밋한 상태로, 배너·대시보드 작업과는 무관하다. 원인은 이관 요청 페이로드 타입에 `archiveOnly: boolean`이 추가됐는데 세 호출부가 갱신되지 않은 것으로 보인다. 결과적으로 `npm run check`가 현재 `main`에서 적색이다. 2026-08-19 배너 작업 중 발견(회귀 재현·베이스라인 확인만 하고 이관 코드는 수정하지 않음). |

_FE-37 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-18 FE-37 사이트 전체 고정 문구 i18n 이관 기록을 참조합니다. 2026-07-26 잔여과제 통합 조치의 완료·폐기 이력은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다. FE-21과 FE-30①·FE-28②는 같은 문서의 2026-08-06 잔여과제 저비용 배치 1 기록을, FE-36은 2026-08-09 잔여과제 일괄 조치 기록을, FE-38은 2026-08-17 DDL 불필요 잔여과제 배치 기록을, FE-41·FE-42는 같은 날 배치 4 기록을, FE-40은 배치 5 기록을, FE-39·FE-43은 배치 6 기록을 참조합니다. FE-44(다국어 관리 조회 실패 표면화)는 2026-08-18 다국어 관리·변경로그 구현 배치에서 처리했습니다(`docs/superpowers/plans/2026-08-18-translation-admin-and-change-log.md` 및 이 배치의 커밋 참조). FE-45(다국어 관리 편집 진입 키보드 접근성)는 전제가 거짓으로 밝혀져 과제에서 제외합니다 — PrimeVue 4.5.5는 Enter/Space 키 활성화도 `onEnterKey → onRowClick` 경로로 `row-click`에 합류시켜 애초부터 키보드로 도달할 수 있었습니다._

## ⚙️ 백엔드

| ID    |     우선순위     | 유형  | 과제                                  | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | :----------: | --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BE-36 |  🟡 Medium   | 계약  | `totRqmAmt` 이름과 값의 불일치 | DB 컬럼 `TPRMPP_BPROJM.TOT_RQM_AMT`는 **총 예산**(∑품목 AMT)이고, 응답 필드 `ProjectDto.Response.totRqmAmt`는 **당해예산**(`∑AMT − ∑MPL_AMT`)이다. 2026-06-22 설계문서가 이미 "컬럼명은 총소요금액이나 실제 저장값은 당해예산"이라 적었고, 2026-08-16 컬럼 재추가 때 총 예산은 새 응답 필드 `prjBgAmt`로 분리해 충돌을 피했다. 현재는 `ProjectResponseMapper.fromEntity`가 엔티티 값을 동명 응답 필드로 흘리지 못하는 구조라 혼선이 차단되어 있으나(최종 리뷰가 세 저장소 전 소비처를 추적해 확인), 이름 자체를 정리하려면 `totRqmAmt` 소비처(예산목록·상세·PDF·전역검색 등 10곳 이상) 전수 조사가 필요하다. |
| BE-24 | 🏛️ External | DB  | 집행 문서 활성 버전 UNIQUE 인덱스의 dev/prod 적용 | 코드·마이그레이션·검증은 2026-08-09에 완료했다([`TASK_DONE.md`](TASK_DONE.md) 2026-08-09 잔여과제 일괄 조치). 남은 것은 **DBA의 dev/prod 적용 하나뿐**이다. 적용 대상은 `it_database/migrations/V20260809_001__AddExecutionDocumentActiveVersionUniqueIndex.sql`(세 테이블에 `IX_TPRMPP_{BDELIM,BCONTM,BPAYMM}_03` 함수 기반 UNIQUE 인덱스), 절차·사전점검·롤백은 `it_database/docs/operations/2026-08-09-be24-active-version-index-handover.md`를 따른다. **사전 점검이 게이트다**: 활성행이 둘 이상인 문서가 있으면 `ORA-01452`로 실패하므로 인계 노트 §2의 중복 조회를 dev/prod에서 먼저 돌려 3건 모두 0인지 확인한다(로컬은 0 확인). 0이 아니면 어느 버전을 남길지 업무 담당자 확인이 선행되어야 하므로 적용하지 않는다. |
| BE-45 |   🟢 Low     | 시드  | 관리자 메뉴 시드가 권한 매핑을 넣지 않음 | `V20260818_002__SeedTranslationAdminMenu.sql`이 `TPRMPP_CMENUA`(메뉴-권한 매핑) 행을 만들지 않는다. `MenuQueryService.isAllowed()`는 "매핑이 0건이면 전체 공개"로 판정하므로, 부모 `MADM0004`가 매핑을 갖지 않은 스키마에서는 비관리자 사이드바에도 노출된다. 서버 권한(`/api/admin/**` → `hasRole('ADMIN')` + 컨트롤러 `@PreAuthorize`)은 온전해 데이터 유출은 없고, 최근 관리자 메뉴 시드 3건(`V20260811_002`, `V20260813_001`, `V20260818_002`)이 모두 같은 패턴이라 회귀도 아니다. 시드 패턴 차원에서 매핑을 함께 넣을지 결정한다. **전제 갱신(2026-08-19)**: 유실됐던 스크립트를 BE-49에서 복원·적용 완료(it_database `c791c63`) — 이제 원래 문면대로 유효하다. |
| BE-47 | 🟠 High | 계약 | `NotificationOutboxService.clamp`가 글자 수로 자르는데 컬럼은 바이트 단위다 | `TPRMPP_CINFMM`을 실측하면 `TTL VARCHAR2(100 **BYTE**)`, `INFM_MSG_CONE VARCHAR2(4000 BYTE)`이고 DB 문자셋은 `AL32UTF8`이다(한글 1자=3바이트). 그런데 `clamp(value, maxLength)`는 `String.length()` 기준이라 `clamp(ttl, 100)`이 통과시킨 한글 100자는 300바이트가 되어 `ORA-12899`로 INSERT가 실패한다. 실패는 `NotificationEventListener`가 삼키고 지표만 올리므로 **알림 행 자체가 조용히 사라진다**(메일·인앱 모두). 결재요청 경로는 제목이 짧아 현재 도달하지 않지만 다른 알림 종류는 도달할 수 있다. `clamp`를 바이트 기준으로 바꾸되 멀티바이트 문자를 중간에서 끊지 않아야 한다 — 같은 문제를 `EaiTextFitter`(2026-08-18)가 `CharsetEncoder` 경계 정지로 이미 푼 선례가 있다. 결재요청 메일 페이로드(`SD_DOC_CONE`)는 2026-08-18 배치에서 바이트 가드를 넣었다. |
| BE-48 | 🟢 Low | 테스트 | 메일 페이로드 트랜잭션 격리에 실제 경계 검증이 없다 | `ApprovalMailPayloadProvider`(비트랜잭션, catch 보유) → `ApprovalMailDataLoader`(`@Transactional(REQUIRES_NEW, readOnly)`) 구조는 메일용 조회 실패가 상신 트랜잭션을 롤백 표시로 오염시키지 않게 하려는 것이다. 현재 검증은 목(mock) 기반이라 트랜잭션 매니저가 없고, 프로바이더에 `@Transactional`을 다시 붙이는 회귀가 들어와도 테스트는 초록으로 통과한다(실제로 2026-08-18 구현 중 catch가 경계 안에 있어 한 번 새어나간 전례가 있다). `./gradlew integrationTest`에 실제 Oracle 기반 검증을 하나 추가한다 — 조회가 던져도 상신이 커밋되고 알림은 null 페이로드로 발행되는지. |
| BE-50 | 🟢 Low | 검증 | `TranslationAdminController.updateTranslations`의 `@Valid` 누락 | 관리자 전용 + 서비스 계층 검증이 있어 보안 문제는 아니나 변경 API `@RequestBody`에 `@Valid`를 적용하는 프로젝트 규칙 위반이고, `translations`가 null인 본문이 서비스까지 내려가 NPE→500이 될 수 있다. `@Valid`와 필드 `@NotNull`을 부여한다. 2026-08-19 REVIEW. |
| BE-51 | 🟢 Low | DB | `TPRMPP_CFILEM` (PK_COL_NM, PK_CONE) 보조 인덱스 | 보조 인덱스가 없어 부모 기준 파일 목록·일괄 삭제가 FULL SCAN이다. 반입 원본 보관(`linkExistingFile`)이 APPLIED 파일당 신청서 수만큼 메타행을 늘리는 첫 대량 소비자이므로, 행 수가 수천 건에 이르기 전 Flyway 신규 버전으로 `IX_TPRMPP_CFILEM_01 (PK_COL_NM, PK_CONE)`을 추가한다(`DEL_YN`은 선택도가 낮아 제외). 2026-08-19 REVIEW. |
| BE-52 | 🟢 Low | 조회 | `RequestFormFileReadAuthorizer.canRead`의 부모당 1+M 조회 | 신청서번호당 `findByApfDcmNo` 1회 + 매핑당 원장 `findById` 1회를 실행한다. 단건·단일 부모는 요청 범위 판정 캐시로 충분하나, `getFilesBatch`가 `편성요청서반입` 종류를 다부모로 부르는 화면이 생기면 부모 수 × (1+M) 쿼리가 된다. 그때 Cappla↔원장 조인 프로젝션(신청서번호→주관부서코드)으로 부모당 1쿼리로 줄이거나 batch 판정 인터페이스를 검토한다. 2026-08-19 REVIEW. |
| BE-53 | 🟢 Low | 문서 | 편성요청서 반입 백엔드 가이드 신설 | `domain.migration` 서브시스템(어댑터 → 검증·진단 코드 → 원장 반영 → 결재 표식 → 원본 보관)의 전용 가이드가 없다. `docs/guides/domains/request-form-import.md`를 신설해 진단 severity 규칙(BLOCKER/WARNING/SKIPPED)과 원본 보관·권한(`file-security.md` 상호 링크)을 담는다. 핵심 규칙은 2026-08-19 REVIEW에서 `it_backend/CLAUDE.md` §8에 반영했다. |
| BE-54 | 🟡 Medium | 카탈로그 | `/admin/translations`·`/admin/migration`·`/admin/migration/requests`의 메뉴 경로 카탈로그 누락 | 세 경로가 메뉴로는 존재하지만 `TPRMPP_CMENUD` 경로 카탈로그 행이 없다. `AdminMenuService.requireUsableCatalogPath()`가 PGE 메뉴 저장 시 카탈로그 행을 요구하므로, 관리자가 `/admin/menus`에서 이 메뉴들을 다시 저장하면 오늘 실패한다. 누락 경로를 카탈로그에 시드하는 마이그레이션이 필요하다. 2026-08-19 배너 작업 중 발견. |

_BE-34 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-06 잔여과제 저비용 배치 1 기록을, BE-35·BE-38·BE-39는 같은 문서의 2026-08-17 DDL 불필요 잔여과제 배치 2 기록을, BE-37·BE-40은 같은 날 배치 3 기록을 참조합니다. BE-41·BE-42·BE-43·BE-44·BE-46은 2026-08-18 다국어 관리·변경로그 구현 배치에서 처리했습니다(`docs/superpowers/plans/2026-08-18-translation-admin-and-change-log.md` 및 이 배치의 커밋 참조). BE-49(다국어 변경로그·메뉴 시드 마이그레이션 유실)는 2026-08-19 당일 복원·적용해 종료했습니다([`TASK_DONE.md`](TASK_DONE.md) 2026-08-19 절 참조)._

## 🔄 수기 엑셀 이관

| ID     |     우선순위     | 유형 | 과제                                   | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | :--------------: | ---- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MIG-01 |     🟡 Medium     | 이관 | 부문계획 `사업진행` 상태의 원장 기록 위치 | **업무 매핑은 확정됐다** — `진행(품의)`→`71`(입찰/계약 요청 작성중), 계약 체결 전→`75`(입찰/계약 진행중), 계약 체결 완료→`79`(입찰/계약 완료). 남은 것은 **어디에 기록할지** 하나다. 실측한 `BPROJA` 관례는 단계 문서 키로 갈린다 — `CNCD_RFR_NO=PRJ-yyyy-nnnn`(사업 자신) 행이 상태 `01`(예산편성 요청 작성중, 157행), `PLN-yyyy-nnnn`(부문계획 문서) 행이 `11`(부문계획 정실협 진행중, 27행)이다. 사업진행은 **사업의 조달 진행**이라 계획 문서 행에 쓰면 의미가 어긋나고, 사업 자신 행에 쓰면 예산편성 요청 상태를 덮는다. 새 키를 두는 방안도 있다. 어느 쪽이든 `ProjectQueryAssembler`의 대표상태 산출이 바뀌어 **여러 화면의 사업 상태 표시가 달라지므로** 도메인 담당자 확인이 선행된다. 도달할 수 없는 매핑 상수를 미리 넣지 않는다(선례: `columns.ts`의 미도달 라벨 제거). **함께 확정할 것 둘**: ① `진행(계약)`은 시트에 체결 전·후 구분이 없어 `75`·`79` 중 하나로 고정해야 한다(현재 해석은 진행 중=`75`). ② `취소(연기)`는 `IT_PTL_STS_TC` 26개 코드에 대응값이 아예 없어 코드값 추가(DML)가 필요하다. 한편 **집행 실적 4열은 금액이라 컬럼이 필요하다(DDL)** — 상태 매핑과 분리해 별도로 다룬다. 종전 항목이 후보로 적었던 `BPROJM.IT_PTL_RPR_STS_TC`는 조회 결과 **보고 대상 직급**(이사회·회장·전무이사·부문장·부서장·기타)이라 후보가 아니다. |
| MIG-27 |      🟢 Low       | 이관 | 반입 선택 후보 목록의 정렬 보장 | `MigrationIoeCatalogReader.candidates(cId, storeName)`가 부르는 `CodeRepository.findByCIdAndDelYn`에 `ORDER BY`가 없어, 결과 표 `결정` 열 선택 상자의 나열 순서가 비결정적일 수 있다. 공통코드에는 순서 컬럼(`CO_C_SQN`)이 있으나 쓰이지 않는다. 값이 틀리게 저장되지는 않는다 — 후보는 `{code, label}` 쌍이고 사용자는 라벨을 보고 고르므로 순서가 흔들려도 고른 값은 정확하다. 이번 기능이 만든 특성이 아니라 `candidates()`를 쓰는 모든 선택 항목(`bzTpC`·`sklTpTc`·`rprStsTc`·`exePttYn`·`ioeC`, 그리고 2026-08-18에 합류한 `curC`·`edrtTc`)의 공통 조건이다. 다만 통화는 시트 ③ 행마다 열리는 선택 상자라 체감 노출이 이전 항목들보다 크다. `findByCIdWithValidDate`는 이미 `CO_C_SQN NULLS LAST, CDVA_ID`로 정렬하므로, MIG-28을 처리하면 통화는 함께 해소된다. |
| MIG-28 |     🟡 Medium     | 이관 | 통화 후보를 유효일자 필터로 읽기 | `GeneralExpenseFormAdapter`가 시트 ③ 통화 후보를 `catalogReader.candidates(CommonCodeGroups.CURRENCY, false)` → `findByCIdAndDelYn`으로 읽는다. 그런데 저장 경로(`CostService.createCost` → `XcrLookupService.resolveXcr` → `findByCIdAndCdvaWithValidDate`)는 **유효일자로 재조회**하고 없으면 `IllegalStateException`으로 롤백한다. `MigrationIoeCatalogReader`의 클래스 JavaDoc과 `MigrationIoeCatalogReaderTest.환율은_유효일자_필터로_읽는다`가 "`CUR_C`에 유효일자를 보지 않는 조회는 아예 쓰지 않는다"고 명시하는데, 그 테스트는 `xcrByCurrency()` 호출 안에서만 `verify(never())`를 걸어 새 호출부를 잡지 못했다. 결과: 유효기간이 닫혔거나 환율(`CO_CDVA_NM`)이 비어 있는 통화가 선택지에 떠서 사전검증은 통과하고 반영에서 그 파일만 `FAILED`로 롤백될 수 있다(`V20260720_008` 주석이 미등록 209종을 두고 경고한 상태). 현재 로컬 시드는 8종 전부 `20260101~99991231`이라 미발현. 리더에 유효일자 기반 후보 메서드를 두고 통화만 그것을 쓰게 한다. **2026-08-19 착수했다가 되돌렸다** — 호출을 바꾸면 `GeneralExpenseFormAdapterTest`·`FormAdapterResolutionPathTest`의 `candidates(CURRENCY, false)` 스텁이 빈 값을 돌려줘 9건이 깨지는데, 그 두 파일은 당시 다른 작업이 미커밋으로 점유(생성자에서 `FormApproverReader` 제거)해 스텁을 고칠 수 없었다. 그 작업이 커밋된 뒤에 리더 메서드 추가 + 두 테스트의 스텁 교체를 한 번에 한다. 2026-08-18 최종 리뷰 I-1. **2026-08-19 갱신**: 차단하던 작업이 `f5171116`으로 커밋되어 착수 가능해졌다(통화 후보 조회는 여전히 `candidates(CURRENCY, false)` — 미해소 확인). |
| MIG-31 |      🟢 Low       | 테스트 | 금액 단위 회귀 방지 테스트 추가 | 통화가 미해석인 행이 남아 있으면 `UNIT_UNCERTAIN`을 생략하지 않는다는 규칙(`GeneralExpenseFormAdapter.resolveUnit`의 `currencies.size() == rows.size()` 조건, 2026-08-19 커밋)에 직접 테스트가 없다. 반대쪽(전 행 외화 확정 → 경고 없음)은 기존 `skipsUnitWarningWhenNoKrwRow`가 지킨다. 테스트가 들어갈 `GeneralExpenseFormAdapterTest`는 당시 다른 작업이 미커밋으로 점유했고 생성자 시그니처가 커밋본(3인자)과 워킹트리(2인자)에서 달라 어느 쪽에 맞춰도 반대쪽이 깨졌다. 그 작업이 커밋된 뒤 "통화 칸이 빈 행이 있고 배수 미지정이면 `UNIT_UNCERTAIN`이 나온다"를 한 건 추가한다. 이 규칙이 깨지면 금액이 1,000배 틀린 채 원장에 들어갈 수 있다. **2026-08-19 갱신**: 차단하던 작업이 `f5171116`으로 커밋되어 착수 가능해졌다(해당 테스트는 아직 없음 — `GeneralExpenseFormAdapterTest`의 기존 `UNIT_UNCERTAIN` 케이스 2건은 전 행 원화·금액 빈 행 시나리오다). |
| MIG-29 |      🟢 Low       | 이관 | 반입 원본 열기 실패 시 무표시 | `RequestFormResultTable`이 `openSourceFile(fileKey)`의 반환값(`false`=선택 목록에 그 파일이 없음)을 버려서, 클릭해도 아무 일이 없는 경우 사용자가 이유를 알 수 없다. 현재 `useRequestFormUpload.selectFiles`가 `clearResults()`를 함께 부르므로 결과가 있는 동안에는 선택 목록도 살아 있어 실질적으로 도달하기 어렵다. 도달 가능해지면 Toast로 안내한다. 2026-08-18 최종 리뷰 M-3. |
| MIG-30 |      🟢 Low       | 이관 | 반입 배치의 공통코드 조회 누적 | 파일 1건마다 `CapitalProjectFormAdapter.catalogs()`가 공통코드 8회, `GeneralExpenseFormAdapter.adapt()`가 `CUR_C` 1회를 읽는다(2026-08-18에 `IT_PTL_EDRT_TC`·`CUR_C` 2회가 늘었다). 조직·비목 인덱스처럼 배치 시작에 한 번 만들어 파일들이 공유하는 구조가 아니라 파일마다 다시 읽는다. 파일 수백 건 배치에서 누적되므로 `OrgIdentityResolver.snapshot()`·`IoeHierarchyIndex.snapshot()`과 같은 자리에서 코드 스냅샷을 만들어 `FormAdapterContext`로 넘기는 방안을 검토한다. 2026-08-18 최종 리뷰 M-4. |
| MIG-12 |      🟢 Low       | 이관 | 편성요청서 국문·영문 대조표 보강 | `FormLexicon`의 라벨·비목 대조표는 런던지점 제출본 1건에서 뽑았다. 다른 해외점포(시드니·뉴욕·도쿄 등)의 번역이 다를 수 있고, 실제로 런던 파일에도 `Machinery` › `Tape backup software`처럼 대응 비목이 애매한 행이 있다. 해외점포 제출본이 들어오면 미해석(`CODE_UNRESOLVED`)으로 남은 어휘를 수집해 대조표에 추가한다. 어휘 추가는 `FormLexicon` 상수 한 줄이면 된다. |

_MIG-02·MIG-04·MIG-06·MIG-15·MIG-19·MIG-20·MIG-21·MIG-23 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-17 DDL 불필요 잔여과제 배치 기록을, MIG-13·MIG-18은 같은 날 배치 3 기록을, MIG-10·MIG-25는 배치 4 기록을, MIG-22·MIG-24는 배치 5 기록을, MIG-03·MIG-14·MIG-16·MIG-17·MIG-26은 배치 6 기록을 참조합니다._

## 🧹 Clean Code 부채

| ID    | 우선순위 | 유형 | 과제                                | 근거/조건 |
| ----- | :------: | ---- | ----------------------------------- | --------- |
| CQ-29 | 🟢 Low | 중복 | 로그 변경유형(chgTp) 표시 로직 4중 중복 | 라벨 함수가 `adminLogPresentation.formatChangeType` ↔ `realtimeLogs.chgTypeLabel`로 이중화(같은 i18n 키, default 처리만 다름)이고, severity 매핑이 `adminLogPresentation.changeTypeSeverity`('U'→'info') / `RealtimeFeedTable.chgSeverity`('U'→'warn') / `RealtimeDetailDrawer.severity`('U'→'warn') 3곳이다. 라벨은 한쪽으로 통일하고 severity는 최소 Realtime 두 곳을 `realtimeLogs.ts`로 승격한다. 'info' vs 'warn' 차이가 의도인지 먼저 확인한다. FE-49의 `chgLabel` 교체와 같은 파일을 만지므로 함께 처리하면 좋다. 2026-08-19 REVIEW. |

_CQ-02~05·07~14 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-21 Clean Code Wave 0·1·2와 2026-07-25 정리 이관 절을, CQ-06·17·21은 2026-08-04 Wave 3 Wave A 절을, CQ-16은 2026-08-04 Wave 3 Wave B·C 절을, CQ-20은 2026-08-01 Prettier 잔여 해소 절을, CQ-25·26은 2026-08-09 잔여과제 일괄 조치 절을 참조합니다. CQ-27·CQ-28은 2026-08-18 다국어 관리·변경로그 구현 배치에서 처리했습니다(`docs/superpowers/plans/2026-08-18-translation-admin-and-change-log.md` 및 이 배치의 커밋 참조)._

## 📝 Tiptap 변수 입력

> 현재 활성 Tiptap 변수 입력 과제 없음. 2026-07-26 완료·폐기 항목과 알려진 제한은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## 📡 실시간 로그

> 현재 활성 실시간 로그 과제 없음.

## 📬 공통 게시판

> 현재 활성 과제 없음.

_BOARD-1(게시판 다국어 메시지의 `@`가 vue-i18n 링크 문법으로 해석돼 500)은 2026-08-19 TEST.md 실행 후속으로 해소했습니다 — `i18n/messages/board.ts`의 ko·en 5개 키를 `{'@'}` 리터럴 보간으로 이스케이프했고, 회귀 방지로 `tests/unit/i18n/messages.test.ts`에 전체 메시지를 vue-i18n 컴파일러에 통과시키는 테스트를 두었습니다. `board.spec.ts` 9건 전부 통과로 확인했습니다._

## 🗂️ 저장소 위생

> 현재 활성 저장소 위생 과제 없음.

_REPO-01 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-17 업무 판단 반영 배치 6 기록을, REPO-02·REPO-03은 같은 날 DDL 불필요 잔여과제 배치 2 기록을 참조합니다._

_2026-08-17 루트 추적 정리: `.agents/`(로컬 에이전트 하네스 설정 4건)·`TaskNotes/`(개인 Obsidian 볼트 12건)·`.review-cache/`(일회성 리뷰 탐지 캐시 9건)·`.superpowers/`(브레인스토밍 런타임 pid·상태 2건)를 `git rm --cached`로 추적 해제하고 `.gitignore`에 등록했습니다. `.superpowers/`는 규칙이 이미 있었는데 추적이 먼저라 무력화된 경우였습니다. 파일은 모두 워킹트리에 남아 있어 로컬 동작에는 영향이 없습니다. `.agents/`의 FP 자료는 손실이 없습니다 — `workflows/Detailed_FP_Estimation_Workflow.md`(105줄)는 추적 중인 `FP/FP.md`(166줄, CSV 산출·생성기 연동 규칙 추가)의 옛 판본이고, `skills/fp/SKILL.md`의 복잡도 매트릭스·점수표도 `FP/FP.md`에 동일하게 있습니다. 나머지 두 스킬(`hookify-rules`, `plankton-code-quality`)은 Codex용 커뮤니티 도구 참조라 이 프로젝트와 무관합니다._
