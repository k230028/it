# IT Portal 잔여과제

|  우선순위   | 의미           |
| :---------: | -------------- |
| 🔴 Critical | 즉시 조치      |
|   🟠 High   | 조속 조치      |
|  🟡 Medium  | 계획 반영      |
|   🟢 Low    | 선택 개선      |
| 🏛️ External | 외부/운영 의존 |
|   ✅ Done   | 확인 완료      |

## 🔒 보안

| ID     | 우선순위 | 유형 | 과제                                                           | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | :------: | ---- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-10 | 🟢 Low   | 의존 | 프론트 `brace-expansion` 1.x/2.x DoS advisory 공식 패치 판정 대기 | GHSA-mh99-v99m-4gvg는 2026-07-24 갱신 상태에서도 영향 버전을 `<=5.0.7`, 패치 버전을 `5.0.8`로만 명시한다. 2026-07-30 npm dist-tags에 `maintenance-v1=1.1.17`, `maintenance-v2=2.1.3`이 공개됐지만 공식 advisory가 두 버전을 패치로 판정하지 않아 의존성을 갱신하거나 완료 처리하지 않는다. 현재 설치 버전은 1.1.16/2.1.2/5.0.8이고 `npm audit`은 `brace-expansion` high를 포함해 총 23건(22 high)을 보고한다. 5.x 라인의 5.0.8 override는 유지하되 CJS named export가 다른 minimatch 계약과 호환되지 않으므로 전역 override를 금지한다. 공식 패치 판정 후에만 소비자별 호환 범위에서 갱신하고 `npm audit`으로 해당 advisory 제거를 확인한다. `npm audit fix --force`는 nuxt/exceljs 다운그레이드를 유발하므로 금지 |

> 완료 항목은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

_SEC-04·SEC-05 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-19 Remediation Phase 1, SEC-06은 같은 문서의 2026-07-19 배포 정합성 후속, SEC-08·SEC-09는 2026-07-25 보안 트랜잭션 무결성 절을 참조합니다._

## 🤝 사전협의

> 현재 활성 사전협의 과제 없음. 2026-07-26 완료·폐기 항목은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## ⚠️ 에러 처리

| ID     |   우선순위    | 유형  | 과제                             | 근거/조건                                                                                                                                                                                                                                                                 |
| ------ | :-------: | --- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ERR-14 | 🟠 High | 안정성 | `useApiFetch`의 401 무한 재조회 폭주 가능성 | `app/composables/useApiFetch.ts`의 `onResponseError` 401 분기(대략 487~503행). `isRefreshing`은 **동시** 401을 합치는 락일 뿐 **순차** 재시도 횟수에 상한이 없다. 서버가 토큰 갱신 후에도 계속 401을 내면 `401 → refresh() 성공 → tokenRefreshSignal++ → 전 인스턴스 재조회 → 401`이 **무한 반복**된다. `tokenRefreshSignal`이 모듈 전역 단일 신호라 한 번 걸리면 그 순간 열려 있는 모든 `useApiFetch` 인스턴스가 동시에 폭주한다. ERR-13(완료, `TASK_DONE.md` 참조)으로 가드가 붙은 지점이 늘어 블라스트 반경이 커졌다. 탭이 동기적으로 멈추지는 않지만(각 사이클이 실제 네트워크 I/O를 거침) 백엔드·클라이언트 양쪽에 무제한 트래픽을 유발하는 자원 고갈 결함이다. 발견 경위: ERR-13 3-keep 작업에서 테스트 대역을 실제 Nuxt 계약에 맞춰 확장했더니 테스트가 OOM으로 죽으면서 드러났다. 401 분기는 인증 흐름이라 ERR-13 작업 지시에서 명시적으로 제외했다. 재시도 횟수 상한 또는 백오프 도입을 검토한다. |
| ERR-15 | 🟡 Medium | 검증  | ERR-13 감사 리포트가 놓친 재조회 미판정 잔여(census 누락) | ERR-13 감사(`docs/superpowers/reports/2026-07-err13-refresh-audit.md`)의 정규식이 `@saved="refreshX"` 형태의 무괄호 템플릿 바인딩을 못 잡아 누락된 지점이다. 전부 "쓰기 성공 후 재조회 미판정"이라는 ERR-13과 **동일한 결함 클래스**다. 잔여 지점: `app/pages/info/council-request/prepare/[id].vue:679,699`(`@saved="refreshCouncil"`), `app/pages/info/council-request/result/[id].vue:910`(동일), `app/pages/info/cost/terminal/[id].vue`(`@saved="refreshCost"`, `TerminalFormDialog` 핸들러). **감사 리포트의 총계 145는 과소 집계다** — 후속 census는 정규식이 아니라 템플릿 바인딩까지 포함해야 한다. 발견 경위: ERR-13 3c·3f 구현자가 각각 발견해 보고, 리뷰어가 확인. 범위 규칙("감사 리포트에 없는 지점을 손대지 마라")에 따라 이번 범위에서 손대지 않았다. **추가(2026-07-31 통합 리뷰)**: `app/pages/admin/logs/[logKey].vue`도 감사 census에서 누락된 것으로 확인됐다(진짜 누락 1건 — `app/pages/budget/summary.vue`·`budget/comparison.vue`는 C-2로 의도적 제외가 이미 등재돼 있어 대상이 아니다). 이 파일의 재조회 지점은 무괄호 템플릿 바인딩이 아니라 `watch(logKey, ...)`(라우트 파라미터 변경, 45·277~280행)와 툴바 새로고침 버튼(`@click="() => refresh()"`, 559~563행)이다 — 놓친 메커니즘은 다르지만(정규식 사각지대가 아니라 파일 자체가 census 대상에서 빠짐) "감사 총계가 과소 집계"라는 같은 결함 클래스라 별도 ID를 만들지 않고 이 항목에 합친다. 이 파일은 `useRefreshGuard`가 아예 없어 재조회 실패와 최초 로드 실패를 화면이 구분하지 못하고(`v-else` 오류 블록이 공용), 배너·재시도 경로도 없다. 후속 census는 정규식뿐 아니라 파일 단위 전수 확인도 병행해야 한다. |

_ERR-09·ERR-10 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-26 에러 표면화·복구 기록을, ERR-06은 2026-07-19 Remediation Phase 1, ERR-05는 같은 문서의 2026-07-19 배포 정합성 후속 기록을 참조합니다. ERR-11·ERR-12는 같은 문서의 2026-07-29 저장 복구·조회 실패 표면화 기록을, ERR-13은 2026-07-31 ERR-13·FE-15~19 조치 기록을 참조합니다._

## 🎨 프론트엔드

> 2026-07-26 잔여과제 통합 조치의 완료·폐기 이력은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

| ID    |  우선순위   | 유형  | 과제                                                             | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | :-----: | --- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FE-15 | 🟢 Low  | 도구  | openapi-typescript 전면 마이그레이션 잔여(파이프라인 도입·1개 도메인 적용은 완료) | 2026-07-31 파이프라인(`npm run codegen`/`codegen:check`)과 `useCost.ts` 1개 도메인 적용 완료(근거는 `TASK_DONE.md`의 2026-07-31 ERR-13·FE-15~19 조치 참조). 전면 마이그레이션 착수 전 걸림돌이 실측으로 드러났다: ① 생성 스키마가 **전부 optional**이라 그대로 적용하면 대량 컴파일 오류가 난다 ② **nullable 필드가 스펙에 반영되지 않는다**(예: `fcAmt`) ③ `openapi-typescript`의 peer dep가 TS6을 지원하지 않아 `--legacy-peer-deps`로 설치했다. **백엔드 OpenAPI 애노테이션 보강(required·nullable)이 선행돼야** 전면 마이그레이션이 가능하다 |
| FE-19 | 🟢 Low  | 도구  | SFC `<style scoped>` 기존 위반 28개 파일 정리(게이트 밖 잔여) | 2026-07-31 FE-19 조치로 `postcss-html` customSyntax를 도입해 `lint:css`가 `app/**/*.{css,vue}`(SFC 포함)를 검사하도록 전환했다. 측정 시점 실측(`npx stylelint "app/**/*.vue" --custom-syntax postcss-html`): 총 580건/37개 파일. 규칙별 상위: `selector-pseudo-class-no-unknown` 157(`:deep`/`:global` — Vue 정당 문법이라 `.stylelintrc.json`의 `*.vue` override에서 `ignorePseudoClasses`로 config 교정, 위반 아님), `selector-class-pattern` 147(vendor 클래스 `.ProseMirror`·`.ML__*` + 프로젝트 BEM 표기 `__`/`--` 혼재), `rule-empty-line-before` 141(포맷팅, `--fix`로 자동 해소 가능하나 이번 태스크 범위(config만) 밖이라 미적용), `color-no-hex` 85(디자인 토큰 미준수 — 규칙 끄지 않음). config 교정 후 잔여 423건/28개 파일은 `.stylelintrc.json`의 `ignoreFiles`에 파일명으로 grandfather 처리했고(신규·미변경 나머지 139개 SFC는 즉시 게이트 적용), 남은 정리는 이 항목에서 추적한다. **재실측(2026-08-01)**: `411건/28개 파일`. 2026-08-01 다이얼로그 개선이 `EmployeeSearchDialog.vue`의 자체 헤더·푸터 마크업과 CSS를 `AppDialogHeader`·`AppDialogFooter`로 대체하면서 45→33으로 12건 줄었고 나머지 27개 파일은 변동이 없다. 파일이 목록에서 빠진 것은 없어 `ignoreFiles` 28개 항목은 그대로다. 아래 목록의 괄호 수치는 최초 측정값이다. **잔여 28개 파일**: `TiptapEditor.vue`(99)·`pages/board/[blbMngNo]/[nacMngNo]/index.vue`(57)·`common/EmployeeSearchDialog.vue`(45→33)·`board/BoardCommentTree.vue`(37)·`common/ProjectListCard.vue`(28)·`VariableNodeView.vue`(19)·`GlobalSearchBar.vue`(18)·`TiptapTableFloatingToolbar.vue`(15)·`common/StyledDataTable.vue`(13)·`AttachmentNodeView.vue`(10)·`budget/BudgetSummaryToggle.vue`(8)·`admin/realtime/RealtimeFeedTable.vue`(8)·`cost/TerminalTableSection.vue`(6)·`pages/info/cost/index.vue`(5)·`pages/budget/summary.vue`(5)·`common/YearPickerTitle.vue`(5)·`InlineMathNodeView.vue`(5)·`BlockMathNodeView.vue`(5)·`pages/info/projects/index.vue`(4)·`pages/info/plan/[id].vue`(4)·`pages/budget/list.vue`(4)·`pages/budget/approval.vue`(4)·`review/ReviewEditor.vue`(4)·`projects/ResourceTableSection.vue`(4)·`AppShell.vue`(4)·`cost/CostFormTableSection.vue`(3)·`pages/diagnosis/index.vue`(2)·`pages/budget/work.vue`(2). 파일별 처리 시 `ignoreFiles` 항목 제거 후 통과를 확인한다. `color-no-hex` 위반은 `docs/guides/styling/design-tokens.md` 토큰으로 치환하고, `selector-class-pattern`은 vendor 클래스(ignoreSelectors 후보)와 자체 BEM 표기(정책 확정 필요 — kebab-case 강제 vs BEM 허용 회귀)를 구분해 처리한다 |
| FE-20 | 🟢 Low  | 제한  | 편집 중 다른 행의 미저장 편집이 재조회로 덮어써짐 | `app/composables/costList/useCostTerminalDialogs.ts:205,208` / `app/composables/costList/useCostExcelTransfer.ts:214`의 재조회가 같은 편집 세션의 **다른 행**(저장 대상이 아닌 행)의 미저장 `_status='modified'` 편집을 덮어쓴다. `useCostEditingState.ts`의 merge watcher가 `_saveError` 보유 행만 보존하기 때문이다(2026-07-31 완료한 FE-16과 같은 보존 로직의 사각지대). 발견·확인: FE-16 구현자가 발견, 리뷰어가 코드로 독립 확인. FE-16은 `onActivated` 억제가 범위였고 이 경로는 "사용자가 명시적으로 일으킨 동작"이라 억제 대상이 아니었다 |
| FE-21 | 🟢 Low  | 경합  | FE-18 census가 놓친 네트워크 자동완성 응답 순서 역전 가드 부재 | `app/composables/useMentionAutocomplete.ts:128-156`(`runSearch`, 게시판 @멘션. 200ms 디바운스 뒤 `$apiFetch`, 가드 없이 `items.value` 직접 대입, 소비처 `BoardCommentTree.vue` 등), `app/composables/useGlobalSearch.ts`(`searchByName`, 헤더 통합검색. 소비처 `app/components/GlobalSearchBar.vue:163`는 원시 `@input`으로 디바운스 없음, 캐시가 빈 첫 검색에서 두 `loadAll()`이 경합할 수 있음). 두 지점 모두 2026-07-31 완료한 FE-18의 census 방법(① `@complete=` 바인딩 grep ② `setTimeout`+`$apiFetch` 조합 grep) 어느 쪽에도 걸리지 않았다. 후속 census는 소비 컴포넌트까지 따라가야 한다 |
| FE-22 | 🟢 Low  | UX  | `useRefreshGuard`에 토스트 억제 옵션 부재 | C-3 지점(`onActivated` 재조회·툴바 새로고침·검색/페이징)에도 실패 시 배너+토스트가 함께 뜬다. `onActivated`는 KeepAlive 재방문마다 실행되므로 백엔드가 지속적으로 400/409/422를 내면 탭을 오갈 때마다 토스트가 반복된다. `it_frontend/CLAUDE.md` §2는 "toast 또는 화면 상태"를 요구하므로 배너만으로도 규약은 만족하지만, `notifyMode` 같은 옵션 추가를 검토한다 |
| FE-23 | 🟢 Low  | 테스트 | `clearNuxtData()`·언마운트 purge 경로의 보존 동작 실측 미검증 | ERR-13 3-keep의 보존 메커니즘은 `clear()`(반환 객체 메서드)에 대해서만 실측 테스트가 있다. `clearNuxtData()`(모듈 전역)와 언마운트 purge는 같은 `clearNuxtDataByKey`를 타므로 구조적으로 동일하다는 추론에만 의존하며, 테스트 대역도 그 두 경로를 구현하지 않는다. 실측 테스트 보강이 필요하다 |
| FE-24 | 🟢 Low  | 제한  | `admin/boards`가 재조회 보존을 잃은 트레이드오프 | `useBoard`가 명시 `key` 없이 `${BASE}/boards/meta`를 열어 공개 게시판 화면과 asyncData 키를 공유한다. 관리자 화면의 보존이 공개 화면까지 새는 것을 막기 위해 ERR-13에서 `enableKeepPreviousData` 노출을 제거했다(`useMenu` 선례). 결과적으로 `admin/boards`는 재조회 실패 시 목록이 빈다(배너는 뜬다). 근본 해결: `useBoard`에 명시 `key`를 주어 화면별로 분리하거나, 관리자 전용 조회를 별도 엔드포인트로 분리한다 |
| FE-25 | 🟢 Low  | UX  | `ResultForm`과 부모의 재조회 실패 배너 2중 노출 가능성 | `app/components/council/result/ResultForm.vue`와 부모 `app/pages/info/council-request/result/[id].vue`가 같은 저장 액션에서 서로 다른 두 조회를 재조회한다. 상관된 원인으로 둘 다 실패하면 배너가 두 개 뜬다(문구는 분리돼 있어 중복 문장은 아니다) |
| FE-26 | 🟢 Low  | UX  | `prepare/[id].vue`의 탭 가시성 한계로 재조회 실패 배너가 안 보일 수 있음 | `onCommitteeSaved`(탭0 "평가위원 선정")가 실패시키는 대상은 탭1에 있는 `ScheduleStatus`의 배너다. `<Tabs>`에 `lazy`가 없어 패널은 마운트되지만 `v-show`로 숨겨져 있어, 탭0에 머무는 사용자에게는 보이지 않는다 |
| FE-27 | 🟢 Low  | 테스트 | 통합 리뷰 B 테스트 위생 잔여(스파이·마운트 누수 등) | 2026-07-31 병합 후 통합 리뷰 그룹 ③(테스트 신뢰도)의 Important I1~I6 중 Critical만 처리(`it_frontend/.superpowers/sdd/integration-fix-B-report.md`)하고 아래는 남겼다: ① 스파이(`vi.spyOn`)가 테스트별로 복원되지 않아 다음 테스트로 누수될 수 있는 지점 ② 대역(mock)이 파일 간 공유돼 격리가 깨지는 지점 ③ 더 이상 소비되지 않는 죽은 mock 배치 ④ `cdvaNm` 분기가 항상 같은 값으로 귀결해 사문화된 지점 ⑤ `stubGlobal`이 `afterEach`에서 수동 복원되지 않는 지점. 사용자 영향이 없는 테스트 코드 위생 문제라 "사용자 영향 + 테스트 신뢰도" 수정 범위(그룹 ③-A~E, Critical) 밖으로 판단해 손대지 않았다. 세부 파일·라인은 통합 리뷰 산출물(`it_frontend/.superpowers/sdd/integration-findings.md` "후속 등재로 넘길 것" 절)에 개별 기록이 남아 있지 않아 착수 시 `tests/unit/`·`tests/integration/` 재조사가 선행돼야 한다 |
| FE-28 | 🟡 Medium | 테스트 | 재조회 가드 회귀 검출 커버리지 공백(`onActivated` 14파일·codegen 드리프트 무테스트) | 통합 리뷰 그룹 ③이 지적한 커버리지 공백(`it_frontend/.superpowers/sdd/integration-findings.md` "후속 등재로 넘길 것" 절): ① KeepAlive `onActivated` → 재조회 가드 경로가 실제로 도는지 관측하는 테스트가 없는 화면이 14개(구체 파일 목록은 리뷰 산출물에 남아 있지 않아 재조사 필요) — 가드·보존 로직이 회귀해도 이 경로에서는 잡히지 않는다 ② `app/composables/useMentionAutocomplete.ts`의 응답 순서 역전 가드 자체는 이미 `TASK.md` FE-21(가드 부재)에 등재돼 있고, 이 지적은 그 가드가 붙은 뒤에도 순서 역전 동작을 검증하는 테스트가 없다는 것이라 별도 ID로 중복 등재하지 않고 FE-21 착수 시 함께 처리한다 ③ `scripts/codegen.mjs`의 스펙 드리프트 판정 로직 자체를 검증하는 단위 테스트가 없다(`npm run codegen:check`가 CI 게이트로는 동작하지만 판정 로직 자체의 단위 테스트는 별개). 회귀를 조용히 통과시킬 수 있는 공백이라 다른 Minor 항목보다 한 단계 높였다 |
| FE-29 | 🟢 Low  | UX  | 통합 리뷰 A Minor 잔여(문구·라벨 불일치) | 그룹 ①·②·④·⑤ 수정(`it_frontend/.superpowers/sdd/integration-fix-A-report.md`) 과정에서 확인했으나 범위 밖(Minor)이라 손대지 않은 것: 재시도 버튼 라벨이 화면마다 통일돼 있지 않음, `app/pages/budget/work.vue`의 toast summary 중복, C-3 지점 안내 문구의 "다시" 유무 혼재(예: "불러오지 못했습니다" vs "다시 불러오지 못했습니다"), 툴바 새로고침 핸들러의 인라인 화살표 형태(`@click="() => refresh()"`)가 화면마다 통일돼 있지 않음, `refreshFailedAfterSave`라는 이름이 실제로는 저장 이후가 아닌 경로에도 쓰여 이름이 실제 사용 범위보다 좁음. 전부 동작에는 영향이 없는 UX 일관성·명명 문제다 |
| FE-30 | 🟢 Low  | 구조  | 통합 리뷰 C Minor 잔여(설정·구조) | 그룹 ⑤ 수정(`it_frontend/.superpowers/sdd/integration-fix-A-report.md`) 과정에서 확인한 것: `useApprovalDashboard`·`useDocumentDashboard`가 `enableKeepPreviousData`를 노출하지 않고 `refresh`만 노출해 소비처가 가드를 만들 수 없는 상태로 남아 있음(의도적 판단인지 미확인), FE-19의 `ignoreFiles` grandfather가 파일 단위라 grandfather 처리된 28개 파일에 새 CSS가 추가돼도 영구히 lint 대상에서 빠짐, `scripts/codegen.mjs`가 ESLint 검사 대상 밖이라 codegen 스크립트 자체는 정적 분석 게이트를 거치지 않음 |
| FE-31 | 🟢 Low  | 문서  | `admin/menus/index.vue` 관리 트리 가드 주석이 보존 도입 후 사실과 다름 | `app/pages/admin/menus/index.vue`의 관리 트리 가드 주석(대략 61~68행)이 "실패하면 `adminTree`가 undefined가 되어 이 화면의 좌측 편집 트리가 비는데"라고 서술하나, `fetchAdminTree()`가 `useApiFetch` 반환 객체를 그대로 돌려주고 페이지가 그것을 `useRefreshGuard`에 넘기므로 가드 생성 시점에 `enableKeepPreviousData()`가 실제로 켜진다. 재조회 실패 시(401·403 제외) `adminTree`는 비워지지 않고 **직전 정상값을 그대로 유지**해 좌측 트리는 빈 트리가 아니라 낡은 값으로 남는다(2026-07-31 통합 리뷰 그룹 ③ 수정 중 실측, `it_frontend/.superpowers/sdd/integration-fix-B-report.md` P-1). 통합 리뷰 그룹 ④(주석 표류)와 같은 유형이지만 ④가 수정한 대상 4곳 목록에 이 파일이 없어 미수정으로 남았다. 배너의 필요성은 여전하다 — 오히려 "낡은 값을 최신으로 오인"하는 방향이라 배너가 더 중요하다. 주석 문구만 `app/components/council/schedule/ScheduleStatus.vue`의 모범형에 맞춰 정정하면 되고 동작 변경은 불필요하다 |
| FE-32 | 🟢 Low  | 테스트 | `createNuxtFetchFake`가 성공 반복 시 같은 객체 참조를 재대입해 `shallowRef`가 트리거되지 않음 | `tests/support/nuxtAsyncData.ts:307`(`data.value = result as T \| undefined;`, `useFetch` 성공 분기)에서 `result`는 `control.value`를 그대로 참조한다. 테스트가 `control.value`를 바꾸지 않은 채 같은 대역으로 성공을 2회 이상 재현하면 `data.value`에 **동일한 객체 참조**가 재대입돼 `shallowRef`가 변경을 감지하지 못한다. 실제 Nuxt/ofetch는 매 응답을 새 객체로 파싱하므로 이 지점은 대역의 충실도 공백이다(2026-07-31 통합 리뷰 그룹 ③ 수정 중 실측, `it_frontend/.superpowers/sdd/integration-fix-B-report.md` 우려사항 C-1). 재현 조건: 같은 `createNuxtFetchFake` 인스턴스로 성공 실행을 2회 이상 일으키고 두 번째 실행에서 `data.value` 갱신(참조 변경 또는 watcher 트리거)을 관측하는 테스트. `useCostPersistence.test.ts`의 취소 복원 테스트와 admin/menus 트리 재조회 성공 케이스는 이미 `control.value`를 새 배열로 갈아 끼우는 방식으로 우회했다. 검증된 공유 대역(별도 리뷰가 "충실도 높음"으로 판정)이라 이번 범위에서 수정하지 않았다 — 고치려면 성공 경로에서 `structuredClone`/얕은 복사로 새 객체를 반환하도록 바꾸되 전 파일 영향(실패 경로의 보존 참조 동일성 단언 포함) 검토가 선행돼야 한다 |
| FE-33 | 🟢 Low  | UX  | 게시판 첨부 재조회가 가드 밖 경로를 거쳐 보존만 되고 배너는 뜨지 않음 | `app/composables/useBoardAttachments.ts`의 `uploadPendingFiles`(238행)와 `deleteAttachment`(275행)가 업로드·삭제 성공 뒤 `useRefreshGuard`의 `attemptRefresh`가 아니라 원시 `refresh()`를 직접 호출한다. 가드 생성 시점(같은 파일 160~170행)에 `enableKeepPreviousData()`가 이미 켜져 있어 이 경로도 실패 시 목록 데이터는 보존되지만, 실패 판정 자체는 `attemptRefresh`가 담당하므로 `refreshFailed` 배너는 절대 세팅되지 않는다. `app/pages/board/[blbMngNo]/[nacMngNo]/edit.vue`는 첨부 삭제 UI가 있어 사용자가 실제로 이 경로를 밟는다(재현: 첨부 삭제 버튼 클릭 → 삭제 자체는 성공 → 뒤따르는 목록 재조회만 실패 → 목록은 낡은 값 그대로지만 "다시 조회" 배너는 뜨지 않는다. 테스트: `tests/unit/pages/refresh-banner-visibility.test.ts:352`). `index.vue`는 첨부 업로드·삭제 UI 자체가 없어 이 경로가 도는 유일한 경우는 `useApiFetch`가 항상 거는 `tokenRefreshSignal` watch(401 → 토큰 갱신 → 투명 재요청)뿐이다(`it_frontend/.superpowers/sdd/integration-fix-A-report.md` 우려사항 2). 통합 리뷰 수정 시 두 화면 모두 테스트가 반환 객체의 `refresh()`를 직접 호출하거나(index.vue, 같은 테스트 파일:300) 실제 삭제 UI를 눌러(edit.vue) 이 경로를 재현해야 했다. 감사 리포트에 없는 지점에 새로 가드를 붙이지 말라는 지시에 따라 고치지 않았다 — 고치려면 두 호출부도 `attemptRefresh`를 거치도록 바꿔야 한다 |

## ⚙️ 백엔드

| ID    |     우선순위     | 유형  | 과제                                                         | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | :----------: | --- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BE-03 |  🟡 Medium   | 성능  | 프로젝션 1차 적용 완료 — 운영 관측 조정·잔여 계약 분리                          | 2026-07-27 1차 적용 완료(6개 묶음 전부 + 가이드 목록 계약 분리, 사용자 승인): it_backend `feature/be03-conservative-projections`(11커밋, `7e73d18`…`b6714db`) + it_frontend `feature/be03-guide-list-split`(2커밋, `c23413a`·`8b45d3a`), 계획 `docs/superpowers/plans/2026-07-27-be03-conservative-projections.md`. **잔여**: ① 운영 관측(AWR/호출량) 후 컬럼 추가 축소 조정 ② Project/Cost wide 검색·알림함 응답 계약 분리 결정 ③ BBUGTM·`ProjectKeyView` 프로젝션 재계획(BE-17 후속) ④ 병합 후 `versions.lock` 갱신(`scripts/update-versions-lock.ps1`). 배포 주의: 가이드 목록 계약 변경(`feat!`)으로 **"신 백엔드+구 프론트" 혼재 구간 금지** — 함께 배포하거나, 간극이 불가피하면 **프론트 선배포 → 백엔드 후배포**(신 프론트는 구 백엔드와 호환: 목록 잉여 본문 무시+단건 API 기존재. 반대 조합은 구 프론트가 빈 본문 PUT으로 데이터 유실 위험). 병합(커밋 이력) 순서는 4-repo 규약대로 백엔드 계약 먼저. 근거: `docs/superpowers/reports/2026-07-be03-projection-survey.md`. |
| BE-29 |  🟡 Medium   | 캐시  | AdminService 공통코드 CRUD의 CacheEvict 공백                      | `AdminService`의 코드 CRUD(`createCode`/`updateCode`/`deleteCode`/`bulkUpsertCodes`)는 `CodeService`와 같은 `CodeRepository`를 쓰지만 `@CacheEvict`가 전혀 없어 관리자 화면에서 코드를 변경해도 `codesByCid`/`budgetPeriod` 캐시가 무효화되지 않는다(1시간 TTL 안전망에만 의존). 2026-07-27 BE-03 조사에서 확인. 영향 범위에 맞는 CacheEvict 적용 필요. **ID 재번호화(2026-08-01)**: 종전 `BE-25`(Medium/DB JPA 식별자 항목과 중복)를 `BE-29`로 옮겼다. `docs/superpowers/plans/2026-07-29-be03-be25-backend-remediation.md`는 `BE-28`로 계획했으나 그 번호는 시퀀스 상한 감시 항목이 이미 쓰고 있다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| BE-26 |    🟢 Low    | 성능  | `AdminService.getOrganizations`의 등록·변경자명 단건 반복 조회(N+1)     | `toOrgResponse`가 행마다 `resolveUserName`을 단건 호출한다. 기존 `UserRepository` 이름 배치 조회(`findNameViewsByEnoIn` 류)로 전환 검토. 조직 12건 규모라 위험도 낮음(2026-07-27 BE-03 조사).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| BE-27 |    🟢 Low    | 테스트 | Oracle IT 공유 조직 fixture(`"120"`) 격리                        | `OrganizationNameProjectionIt`·`UserReadProjectionIt`·`CommitteeUserProjectionIt`가 같은 조직코드 `"120"` 행을 각자 upsert/변형해 공유한다. 현재 순차 실행에서는 안전하나 Gradle 병렬 테스트 도입 시 경합 위험 — 클래스별 접두사 코드로 격리 검토(2026-07-27 BE-03 리뷰 지적).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| BE-30 |    🟢 Low    | 성능  | `getSummary` 표시명 병합 블록의 잔여 encounter-order 채택 2곳           | BE-17 최종 리뷰에서 발견된 계획 범위 외 잔여 지점: `BudgetWorkService.getSummary`의 표시명 병합 블록에서 대표 ioeC를 `ioeCodes.get(0)`으로, 편성률을 `allRecords...findFirst()`로 채택한다(2026-07-27 기준 약 588·622행, `c5be086` 이전부터 존재). BE-17 결정 #2와 같은 원칙(대표 편성행 기준)으로 정리 검토. 위험도 낮음 — 같은 표시명 그룹 내 값 차이가 드묾. **ID 재번호화(2026-08-01)**: 종전 `BE-24`(High/DB 집행문서 항목과 중복)를 `BE-30`으로 옮겼다. `docs/superpowers/plans/2026-07-29-be03-be25-backend-remediation.md`는 이 항목을 `BE-29`로 옮기기로 계획했으나 그 사이 `BE-28`(시퀀스 상한 감시)과 `BE-29`(캐시)가 채워져 다음 번호를 쓴다. `docs/superpowers/plans/2026-07-29-clean-code-wave3-remaining-debt.md` §195의 "BE-24(잔여 encounter-order 2곳)"도 이 항목을 가리킨다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| BE-18 | 🏛️ External | 정리  | P2 구 검토자 API 경로 제거                                         | 재개 조건: 전역 `/api/reviews/reviewers` 경로가 운영에 1릴리스 배포된 후 14일간 구 경로 WARN 호출 0건. 조건 충족 시 제거 대상: `ReviewerController.getReviewersLegacy`(`/{docMngNo}/reviewers` 라우트 포함)와 `ReviewerControllerTest.getReviewers_구경로호환_200`. 충족 전에는 호환 경로와 WARN 관측 유지.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| BE-19 |  🟡 Medium   | 검증  | `ProjectDto.CreateRequest.abusTc`에 `@NotBlank` 추가 검토       | `it_backend/src/main/java/com/kdb/it/domain/budget/project/dto/ProjectDto.java:217`에 검증 어노테이션이 없어 의미상 누락된 사업구분 값이 서비스 계층에서 `CodeDefaults.orNotApplicable()`로 조용히 `'0'`(해당없음)으로 보정된다(`ProjectDto.java:272`). 누락과 고의적인 '해당없음' 선택을 구분하려면 입력 단계 검증이 필요하다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| BE-20 |  🟡 Medium   | DB  | `TPRMPP_CINFMM.INFM_SD_STS_C` 운영 DEFAULT `'10'`이 앱 코드셋에 없음 | 운영 컬럼 DEFAULT는 `'10'`이나 앱이 실제 쓰는 코드셋은 `Cinfmm.DISPATCH_PENDING`/`SENT`/`FAILED`(01/02/03)뿐이라 DEFAULT가 사실상 도달 불가능한 값이다. 운영과 앱 코드셋의 정합성을 DBA와 함께 확인해야 한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| BE-21 |    🟢 Low    | 정책  | `TPRMPP_CDECIM.DCD_TP_C`가 결재선 전 행에 `'10'`(요청)으로 일괄 부여됨     | `ApplicationService.java:189`가 최종결재자를 포함한 결재선 전 행에 `Cdecim.DECISION_TYPE_REQUEST`를 부여해 최종결재자에게도 '요청'이 들어간다. 현재 이 컬럼을 읽는 코드가 없어 무해하나, 결재유형이 실제 의미를 갖는 기능이 추가되면 재검토가 필요하다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| BE-22 |   🟠 High    | 운영  | dev/prod 마이그레이션 적용 전 DBA 인계 노트                             | (a) `V20260724_001`의 `TPRMPP_BESTTM` PK DROP/ADD는 기존 PK가 이미 올바르더라도 무조건 재생성해 유지보수 창에서 비용이 발생한다. (b) `V20260724_002` 적용 전 `TPRMPP_BBIZCM`/`TPRMPP_BBIZCL`.`NOW_CTT_MANR_C` NULL 건수를 반드시 사전 확인해야 한다 — 있으면 RAISE_APPLICATION_ERROR 가드가 마이그레이션 전체를 중단시킨다. 가드에 의해 중단된 경우 Flyway가 실패 시도를 이력에 남겼을 수 있으므로 재적용 전 `flyway repair`가 필요할 수 있다. NULL을 채운 뒤 재실행하는 것은 안전하다 — backfill은 `WHERE ... IS NULL` 조건의 멱등 갱신이고 두 `ALTER ... DEFAULT` 문도 멱등이며, `align_nullable`은 `ALL_TAB_COLUMNS.NULLABLE`을 먼저 확인한 뒤에만 변경한다. (c) `*L` 변경로그 테이블의 backfill은 "당시 값 없음"을 코드값으로 덮어쓰는 이력 변경이므로 감사 이력 관점의 사전 공지가 필요하다.                                                                                                                                                                                     |
| BE-23 | 🏛️ External | 운영  | SSO 벤더 배포물(`it_backend/sso/`) 사내 아티팩트 저장소 이전               | 보관 위치 팀 합의 필요. 현재 상태: 로컬 사본만 유지 중이며 git 추적 해제 완료, `sso/README.md`(보존 거버넌스)만 추적 유지 (2026-07-26 아키텍처 리뷰 이행 #8 후속)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| BE-24 |   🟠 High    | DB  | 집행 문서 3종의 활성 최신 버전 단일성 보장                                  | `DeliberationRepositoryImpl.java:128-129`, `ContractRepositoryImpl.java:127-128`, `PaymentRepositoryImpl.java:127-128`은 `(DOC_MNG_NO, LST_YN='Y', DEL_YN='N')`을 단건으로 읽지만 물리 PK는 `(DOC_MNG_NO, DOC_VRS_SNO)`라 활성 최신행 1건을 보장하지 않는다. 기존 중복 데이터를 정리한 뒤 Oracle 함수 기반 UNIQUE 인덱스 또는 동등 제약을 추가하고, 버전 전환을 잠금·원자 갱신으로 처리한다. Oracle 통합 테스트에서 중복 활성행 거부와 목록·상세 단건성을 검증한다.                                                                                                                                                                                                                                                                                                                                                                                                              |
| BE-28 |   🟠 High    | DB  | `%04d` 채번 시퀀스 15건의 9,999 상한 도달 감시                          | `V20260730_003__NormalizeSequenceMaxValues.sql`이 채번 포맷의 zero-padding 폭을 기준으로 MAXVALUE를 통일하면서 `%04d` 계열 15건(`BASCTM`·`BBUGTM`·`BCONTM`·`BCOSTM`·`BDELIM`·`BESTIM`·`BGDOCM`·`BITEMM`·`BPAYMM`·`BPLANM`·`BPROJM`·`BRDOCM`·`BTERMM`·`CBLBCM`·`CBLBMM`)의 상한이 9,999로 좁혀졌다(적용 전 상한은 10^5~10^28). 이 시퀀스들은 연도별로 초기화되지 않는 **영구 누적**이므로 누적 9,999건에서 `ORA-08004`로 채번이 실패한다. 특히 게시물(`CBLBCM`)과 사업 품목(`BITEMM`)은 도달 가능성이 있다(2026-07-30 기준 각 410·437). **감시**: `SELECT sequence_name, last_number FROM all_sequences WHERE sequence_owner='ITPOWN'`으로 주기 확인. **해소 선택지**: ① 채번 포맷을 `%07d`로 넓히고 MAXVALUE 동반 확대 ② 연도별 시퀀스 분리 ③ MAXVALUE만 컬럼 수용 한도로 환원(`ALTER SEQUENCE ... MAXVALUE`, 값 손실 없음). 단 `BBUGTM`은 Oracle `LPAD(SQ_TPRMPP_BBUGTM_1.NEXTVAL, 4, '0')`(`BbugtmRepository.java`)로 채번해 초과분을 잘라내므로 번호가 조용히 충돌한다 — 포맷을 먼저 넓히지 않고 MAXVALUE만 늘리면 안 된다. **정정(2026-08-01)**: 종전 이 문장이 `CMENUM`을 함께 묶었으나 실제로는 `CmenumRepositoryImpl.nextMnuId()`가 `"MNU" + String.format("%07d", seq)`를 쓴다(SQL `LPAD` 아님). Java `String.format`은 자릿수를 넘어도 자르지 않고 늘어나므로 조용한 충돌은 없고, `MNU_ID` 컬럼 길이를 넘기면 저장 시 실패한다. `SQ_TPRMPP_CMENUM_1`의 MAXVALUE도 9,999가 아니라 9,999,999이며 이 항목의 `%04d` 15건 목록에 포함되지 않는다. 운영 소스 전체에서 SQL `LPAD` 채번은 `BBUGTM` 한 곳뿐이다. |
| BE-31 |  🟡 Medium   | 문서  | Swagger `description`에 남은 파일 API 레거시 파라미터명 정리(codegen 재생성 동반)  | 공통 첨부파일 API의 실제 파라미터는 `pkColNm`·`pkCone`·`flTpCone`(`FileDto.SearchCondition`/`UpdateRequest`)인데, `GuideDocController.java`의 `@Operation(description=...)` 2곳(생성 89행, 삭제 126행)이 아직 `orcDtt=가이드문서`, `orcPkVl={docMngNo}`로 안내한다. 이 문자열은 OpenAPI 스펙을 타고 프론트 생성물 `app/types/api.d.ts`(220·1003행)에 그대로 실려 있어 소비자가 잘못된 파라미터명을 따라 쓰게 된다 — 실제로 `app/pages/guide/index.vue`가 `query: { orcDtt: '가이드문서' }`로 호출해 첨부 목록이 400으로 실패했고 2026-08-01 `fe5be3b`에서 `pkColNm`으로 고쳤다(회귀 테스트 `tests/unit/pages/guide-attachment-error.test.ts`). **이번 REVIEW(2026-08-01)에서 순수 JavaDoc·주석은 정정했으나**(`FileService`, `GuideDocController` 클래스·메서드 JavaDoc, `CouncilApprovalService`, `CouncilNotice.vue`) `@Operation` 문자열은 손대지 않았다 — 이를 바꾸면 OpenAPI 스펙이 달라져 백엔드를 기동한 뒤 `npm run codegen`으로 `api.d.ts`를 재생성해야 하고, 그전까지 `npm run codegen:check`가 드리프트로 실패한다. 백엔드 계약 커밋을 먼저 만들고 프론트 재생성 커밋을 뒤이어 만드는 4-repo 규약대로 함께 처리한다. 잔여 확인: `grep -rn "orcDtt\|orcPkVl" it_backend/src it_frontend/app`(예산작업 도메인 `BudgetWorkDto`/`BudgetWorkService`의 `orcPkVl`은 BBUGTM의 실제 필드명이므로 대상이 아니다). |
| BE-32 |  🟡 Medium   | 정책  | `CouncilService.PRJ_STS_COUNCIL_DONE = "39"`의 의미 확정 | 2026-08-03 `GET /api/council` 0건 조사에서 발견한 잔여 드리프트. 같은 클래스의 `PRJ_STS_COUNCIL_IN_PROGRESS`는 존재하지 않는 코드 `'32'`였고 `'45'`(타당성검토 정실협 진행중)로 정정했으나, `PRJ_STS_COUNCIL_DONE`은 JavaDoc이 "타당성검토 정실협 완료"라고 적혀 있는데 값은 `'39'`(요구사항 구체화 완료)다. 실제 `IT_PTL_STS_TC` 코드셋에서 타당성검토 정실협 완료는 `'49'`다. 한편 이 상수를 쓰는 `skipCouncil`·`notifyCouncil`의 메서드 JavaDoc은 "사업 상태를 '요건 상세화'로 변경"이라고 안내해 `'31'`(요구사항 구체화 진행중)에 가깝다 — 상수 JavaDoc·값·메서드 JavaDoc 셋이 서로 다르다. 협의회 목록 조회는 `'09'`/`'45'`만 필터하므로 어느 값이든 목록 동작에는 영향이 없어 이번 수정 범위에서 제외했다. 업무 담당자와 통보·생략 이후의 사업 상태를 확정한 뒤 상수와 양쪽 JavaDoc을 한 번에 맞춘다. **로컬 데이터 잔재(참고)**: 같은 재정렬의 여파로 로컬 시드의 `ASCT-2026-0401`·`ASCT-2026-0402`는 신청된 협의회인데 사업 자신의 BPROJA 행이 `'45'`가 아닌 `'09'`로 남아 목록에 뜨지 않고, `ASCT-2026-0400`은 `BPROJM`에서 `DEL_YN='Y'`인 `PRJ-2026-0414`를 참조해 어떤 조건으로도 조인되지 않는다. |
| BE-33 |   🟠 High    | 정책  | `ProjectService`의 사업 '대표상태'가 다른 단계 문서에 가려짐 | 2026-08-03 `GET /api/council` 0건 조사에서 발견한 인접 결함. `ProjectService.representativeStatus`(1372~1379행)가 한 사업의 BPROJA 행 전체에서 `IT_PTL_STS_TC` 최댓값을 대표상태로 채택하고 목록 응답에 주입한다(1129~1136행). 그러나 BPROJA는 `(ABUS_MNG_NO, CNCD_RFR_NO)` 단위 **단계 문서별** 상태 테이블이라 사업 자신의 상태는 `CNCD_RFR_NO = ABUS_MNG_NO`인 행뿐이고, 나머지는 상위 계획(`PLN-...`)·사업계획(`BIZ-...`) 등 다른 문서의 상태다. 2026-08-03 로컬 기준 21개 사업 전부가 자신의 행은 `'09'`인데 상위 계획 행 `'11'`을 함께 갖고 있어 대표상태가 전부 `'11'`(정보기술부문계획 정실협 진행중)로 계산된다 — 사업 자신의 단계와 무관한 값이 목록에 표시된다. 협의회 목록(`CouncilRepository`)은 같은 원인으로 0건이었고 사업 자신의 행만 조인하도록 고쳤으나, 이쪽은 "대표상태"의 업무 정의(사업 자신의 단계인가, 관련 문서 중 가장 진척된 단계인가)를 먼저 확정해야 해 이번 범위에서 제외했다. 정의 확정 후 Oracle 통합 테스트로 다른 단계 문서가 섞여도 값이 흔들리지 않음을 검증한다. |
| BE-32 |  🟡 Medium   | 정책  | `CouncilService.PRJ_STS_COUNCIL_DONE = "39"`의 의미 확정 | 2026-08-03 `GET /api/council` 0건 조사에서 발견한 잔여 드리프트. 같은 클래스의 `PRJ_STS_COUNCIL_IN_PROGRESS`는 존재하지 않는 코드 `'32'`였고 `'45'`(타당성검토 정실협 진행중)로 정정했으나, `PRJ_STS_COUNCIL_DONE`은 JavaDoc이 "타당성검토 정실협 완료"라고 적혀 있는데 값은 `'39'`(요구사항 구체화 완료)다. 실제 `IT_PTL_STS_TC` 코드셋에서 타당성검토 정실협 완료는 `'49'`다. 한편 이 상수를 쓰는 `skipCouncil`·`notifyCouncil`의 메서드 JavaDoc은 "사업 상태를 '요건 상세화'로 변경"이라고 안내해 `'31'`(요구사항 구체화 진행중)에 가깝다 — 상수 JavaDoc·값·메서드 JavaDoc 셋이 서로 다르다. 협의회 목록 조회는 `'09'`/`'45'`만 필터하므로 어느 값이든 목록 동작에는 영향이 없어 이번 수정 범위에서 제외했다. 업무 담당자와 통보·생략 이후의 사업 상태를 확정한 뒤 상수와 양쪽 JavaDoc을 한 번에 맞춘다. **로컬 데이터 잔재(참고)**: 같은 재정렬의 여파로 로컬 시드의 `ASCT-2026-0401`·`ASCT-2026-0402`는 신청된 협의회인데 사업 자신의 BPROJA 행이 `'45'`가 아닌 `'09'`로 남아 목록에 뜨지 않고, `ASCT-2026-0400`은 `BPROJM`에서 `DEL_YN='Y'`인 `PRJ-2026-0414`를 참조해 어떤 조건으로도 조인되지 않는다. |
| BE-25 |  🟡 Medium   | DB  | JPA 식별자와 물리 복합 PK 정합화                                      | `Cappla`, `Bcmmtm`, `Bmqnam`, `Bpqnam`의 `@Id`가 각각 물리 PK 일부만 표현한다(`it_database/ITPOWN_DDL_live.sql`의 CAPPLA·BCMMTM·BMQNAM·BPQNAM PK 참조). 업무 키를 확정해 `@IdClass`/`@EmbeddedId`와 Repository ID 타입을 물리 PK에 맞추거나, 앱 식별자 단독 UNIQUE/PK를 DB에 명시한다. Oracle 통합 테스트로 동일 부분키 다중행의 조회·수정·삭제 격리를 검증한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## 🧹 Clean Code 부채

| ID    |   우선순위    | 유형    | 과제                                                                          | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | :-------: | ----- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CQ-01 | 🟡 Medium | 리팩터링  | 대형 서비스·컨트롤러를 도메인 경계별로 분해                                                    | 착수 트리거: 대상 4개(`ProjectService`, `CostService`, `BudgetWorkService`, `CouncilController`) 중 해당 도메인의 기능 변경 착수 시, 같은 계획에 Query/Command(조회·변경) 분리와 모듈별 컨트롤러·테스트 분리를 포함해 수행한다. 단독 빅뱅 분해는 하지 않는다 (2026-07-21 Clean Code 로드맵 Wave 3 확정). 출처: `docs/clean-code-review-2026-07-07.md` §5 4순위, `docs/superpowers/specs/architecture-review-2026-07-08.md` #10 후속 위임(2026-07-26 재실측 `ProjectService` 1,478줄·`CouncilController` 1,279줄·`CostService` 1,142줄·`BudgetWorkService` 1,119줄 — 리뷰 시점 대비 증가 중) |
| CQ-15 | 🟡 Medium | 리팩터링  | 프론트 잔여 800줄 초과 파일 분해                                                        | 2026-07-29 frontend `937f5f9` 재실측 기준 운영 소스 30개. Wave B에서 신규 800줄 상한과 기존 파일별 증가 불가 ratchet을 먼저 도입한다. Wave C 즉시 실행분은 HWPX 1개, editor 5개, composable 2개를 public facade 유지 방식으로 분해해 기준을 30→22로 줄이고, 남은 page 20개와 component 2개는 기능 변경 PR마다 최소 한 관심사를 추출하는 trigger 방식으로 0까지 상환한다. 실행 SoT: `docs/superpowers/plans/2026-07-29-clean-code-wave3-wave-c-frontend-decomposition.md` 및 연결된 C-1·C-2·C-3 child plan |
| CQ-16 |  🟢 Low   | 리팩터링  | `components/` 루트 평면 파일을 `components/editor`·`components/layout` 신설 디렉터리로 이동 | **범위 확정** — editor 14개와 layout 10개를 이동하고 production 소비자는 모두 명시적 import로 전환한다. root에는 CQ-18 후보 4개만 남기며 architecture test로 경계 재발을 차단한다. 이동 전 `max-lines` ratchet을 도입하고 editor → layout 순서의 독립 커밋으로 수행한다. 실행 SoT: `docs/superpowers/plans/2026-07-29-clean-code-wave3-wave-b-frontend-structure.md` |
| CQ-18 |  🟢 Low   | 리팩터링  | `components/` 루트 잔여 범용 UI 4개의 `components/common` 편입 판단                      | CQ-16 범위 축소로 분리(2026-07-29). 대상은 `PageHeader`·`AppDialogFooter`·`TableCard`·`AppDialogHeader`로 `editor`·`layout` 어디에도 속하지 않는 범용 UI다. 다수 화면이 자동 등록명으로 소비하므로 `common/` 편입 시 각 소비 파일에 명시적 import가 필요하다. 기능 브랜치에서 참조가 계속 변하므로 과거의 고정 참조 수를 재사용하지 않고, 진행 시 컴포넌트별 소비 manifest를 새로 만든 뒤 하나씩 분할 실행한다. 로드맵: `docs/superpowers/plans/2026-07-29-clean-code-wave3-remaining-debt.md` §3 B-1c |
| CQ-19 |  🟢 Low   | 리팩터링  | `Btermm.update`의 15개 위치 인자를 명시적 명령 객체로 전환할지 판단                           | CQ-06 크로스체크에서 별도 패턴으로 확인했다. production 호출부는 `CostService.java:383`, 단위 테스트 verify는 `CostServiceTest.java:832`다. CQ-06과 섞지 않고 해당 비용 단말기 기능 변경 시 `@Builder UpdateCommand` 전환의 이득·필드 기본값·테스트 범위를 먼저 확인한다. CQ-06 완료가 선행되지만 기능상 blocker는 아니다. |
| CQ-20 |  ✅ Done  | 포맷    | `format:check` 잔여 위반 3개 파일 정리                                              | ERR-13·FE-15~19 브랜치 이전부터 남아 있던 Prettier 포맷 드리프트: `app/composables/useAuth.ts`, `app/types/menu.ts`, `tests/unit/composables/useAdminMenu.test.ts`. **2026-08-01 확인**: 세 파일 모두 `npx prettier --check`를 통과해 해소됐다. 같은 시점 `npm run format:check`가 보고한 다른 2건(`tests/unit/composables/useLatestRequest.test.ts`, `tests/unit/features/project/useProjectFormCodes.test.ts`)은 커밋되지 않은 작업 트리 파일(각각 미커밋 수정·미추적)이라 이 항목의 커밋된 부채가 아니다 — 해당 작업을 커밋하기 전에 `npx prettier --write`로 정리한다. |

| CQ-21 |  ✅ Done  | 포맷    | 백엔드 `spotlessCheck` 위반 — `./gradlew check` 게이트가 main에서 실패 | **2026-08-04 해소.** 2026-08-01 REVIEW 시점의 대상 파일은 `domain/menu/repository/CmenumRepositoryCustom.java`였으나, Wave A 착수 시 재실행한 `./gradlew check`에서 실제 잔여 위반은 `domain/budget/project/repository/ProjectItemRepository.java`의 `hasSecuritySystemItem()` JavaDoc 줄바꿈 1건이었다(작업 트리 미수정, HEAD 상태 그대로). `./gradlew spotlessApply` 후 해당 파일만 별도 커밋(it_backend `3f65cf5`)했고, 같은 시점 `./gradlew check`가 `BUILD SUCCESSFUL`로 통과해 백엔드 품질 게이트가 main에서 복구됐다. |

_CQ-02~05·07~14 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-21 Clean Code Wave 0·1·2와 2026-07-25 정리 이관 절을 참조합니다._

## 📝 Tiptap 변수 입력

> 현재 활성 Tiptap 변수 입력 과제 없음. 2026-07-26 완료·폐기 항목과 알려진 제한은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## 📡 실시간 로그

| ID     | 우선순위 | 유형 | 과제                                                 | 근거/조건                                                                                               |
| ------ | :------: | ---- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| LOG-03 |  🟢 Low  | 확장 | SSE 또는 WebSocket push 실제 전환                    | 운영 임계치와 feature flag 설계는 문서화 완료. 관리자 수·트래픽 증가 시 별도 기능으로 구현              |
| LOG-04 |  🟢 Low  | 운영 | 로그 데이터 보존 정책 시행과 아카이브 운영 절차 확정 | 테이블 분리 없이 View/운영 설정 기준 문서화 완료. 실제 보존기간·아카이브 운영 기준은 운영 의사결정 필요 |

## 📬 공통 게시판

| ID     | 우선순위  | 유형 | 과제                                             | 근거/조건                                                              |
| ------ | :-------: | ---- | ------------------------------------------------ | ---------------------------------------------------------------------- |
| BRD-02 |  🟢 Low   | 성능 | 본문 검색 Oracle Text 인덱스 도입                | 게시판당 1만 건 또는 검색 1초 초과 시                                  |
| BRD-03 |  🟢 Low   | 성능 | 조회수 카운터 Redis 전환                         | 다중 인스턴스 운영 시                                                  |
| BRD-05 |  🟢 Low   | 기능 | 첨부파일 다운로드 카운트 컬럼(`FL_DWN_NBR`) 추가 | 자료실 인기 자료 통계                                                  |
| BRD-06 |  🟢 Low   | 기능 | 댓글 첨부파일 지원                               | `ORC_DTT="공통게시판댓글"` 추가                                        |

## 🔌 EAI

| ID     |  우선순위   | 유형 | 과제                                                                          | 근거/조건                                                               |
| ------ | :---------: | ---- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| EAI-01 | 🏛️ External | 운영 | KDB EAI 운영팀으로부터 IT Portal 전용 IF_ID 및 UMS 템플릿 발급·확정           | 시스템 식별자 IPP/PRM/PP는 프로퍼티로 확정                              |
| EAI-02 | 🏛️ External | 운영 | 운영 프로파일에서 `eai.enabled=true` + `eai.url` 주입 및 배포 체크리스트 반영 | 코드 설정은 환경변수 기반. 실제 운영 `EAI_URL` 주입·검증 필요           |
| EAI-05 | 🏛️ External | 운영 | 발신채널 상수 IT Portal 발신처 교체 필요성 확인                               | 현재 값: `1588-1500`, `hrd@kdb.co.kr`                                   |
| EAI-06 | 🏛️ External | 운영 | GWE 전문 실제 규칙 KDB 확인                                                   | `RMS_SYS_C`, `IF_ID`, `MSG_KEY` 접두, 발신자 상수, `SYSTEM_CODE` 운영값 |
| EAI-07 |   🟢 Low    | 확장 | 신규 시스템 연동 시 EaiPayload + EaiPayloadSection 1쌍 추가 패턴 유지         | 플러그형 확장 규칙                                                      |
