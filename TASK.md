# IT Portal 잔여과제

|  우선순위   | 의미           |
| :---------: | -------------- |
| 🔴 Critical | 즉시 조치      |
|   🟠 High   | 조속 조치      |
|  🟡 Medium  | 계획 반영      |
|   🟢 Low    | 선택 개선      |
| 🏛️ External | 외부/운영 의존 |
|   ✅ Done   | 확인 완료      |

## 🔒 보안

| ID     |     우선순위     | 유형 | 과제                                  | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | :--------------: | ---- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-12 |     🟢 Low      | MFA  | OnePass FIDO 사용자 거부 상태 구분    | `trResultConfirm`이 `resultCode=100000`이면서 `trStatus != 1`인 응답을 모두 미결정(UNDECIDED)으로 본다. 연동 규격에 사용자 거부를 뜻하는 `trStatus` 값이 정의되어 있지 않아 거부와 대기를 구분하지 못하며, 거부한 거래도 challenge 만료(90초)까지 미결정으로 남는다. 규격에 거부 상태값이 추가되면 `OnePassClient.confirmFido`의 분기에서 `failure()`로 분리한다.                                                     |
| SEC-17 |     🟠 High      | 게시판  | 게시물 목록 검색조건의 `ignorePublicationPeriod`가 클라이언트에서 바인딩된다 | `BoardPostDto.SearchCondition.ignorePublicationPeriod`는 서버 전용 플래그로 설계됐다 — 무인자 메서드 `ignorePublicationPeriod()`로만 켜도록 두고 `@Schema(hidden = true)`로 문서에서도 감췄다. 그러나 클래스 레벨 Lombok `@Setter`가 `setIgnorePublicationPeriod(boolean)`을 생성하고 `BoardPostController.searchPosts`가 `@ParameterObject @ModelAttribute`로 바인딩하며, 저장소 전체에 `@InitBinder`·`setDisallowedFields`가 **한 건도 없다**(실측 0건). `@Schema(hidden=true)`는 OpenAPI 노출만 막고 바인딩은 막지 않는다. 따라서 비관리자가 `GET /api/boards/{blbMngNo}/posts?ignorePublicationPeriod=true`를 호출하면 `BoardPostRepositoryImpl:130`의 공개기간 조건(`STT_DT`·`END_DT`)이 통째로 빠져 **아직 공개되지 않았거나 기간이 지난 게시물이 목록에 노출**된다. `xpoYn='Y'` 조건은 바깥에 남아 있어 화면노출 OFF 게시물까지 새지는 않는다. 조치는 이 필드를 `@Setter(AccessLevel.NONE)`으로 제외하거나 `SearchCondition`에서 분리해 서비스가 별도 인자로 넘기는 것이며, 쿼리 파라미터 주입이 무시되는지 고정하는 회귀 테스트를 함께 둔다. 2026-08-23 REVIEW. |

_SEC-04·SEC-05 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-19 Remediation Phase 1, SEC-06은 같은 문서의 2026-07-19 배포 정합성 후속, SEC-08·SEC-09는 2026-07-25 보안 트랜잭션 무결성 절을 참조합니다._

_SEC-10(지정맥 BioAgent 연동 규격 반영)과 SEC-11(지정맥 해시 검증 도입)은 `mfa.md` 반입 직후 같은 작업에서 해소해 표에 남기지 않습니다. 규격과 구현 대조 결과는 `it_frontend/README.md`의 「추가 인증(MFA) 연결」을 참조합니다._

## 🤝 사전협의

> 현재 활성 사전협의 과제 없음. 2026-07-26 완료·폐기 항목은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## ⚠️ 에러 처리

> 현재 활성 에러 처리 과제 없음.

_ERR-09·ERR-10 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-26 에러 표면화·복구 기록을, ERR-06은 2026-07-19 Remediation Phase 1, ERR-05는 같은 문서의 2026-07-19 배포 정합성 후속 기록을 참조합니다. ERR-11·ERR-12는 같은 문서의 2026-07-29 저장 복구·조회 실패 표면화 기록을, ERR-13은 2026-07-31 ERR-13·FE-15~19 조치 기록을, ERR-14는 2026-08-06 잔여과제 저비용 배치 1 기록을 참조합니다._

## 🎨 프론트엔드

| ID    |  우선순위  | 유형 | 과제                                            | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | :--------: | ---- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FE-47 | 🟢 Low | 테스트 | 정보 홈·보고서 E2E 6건 — 원인 조치 후 재실행 확인 | **2026-08-22 원인 규명 완료. 두 원인 모두 조치했고 남은 것은 E2E 재실행 확인이다**(이 환경에서는 `auth.setup.ts`가 수동 로그인(`headless:false`)을 요구해 실행할 수 없었다). ① **정보 홈** — 가설대로 미mock 호출이 실제 백엔드(28080)로 새어 401 → 로그아웃 경로였다. 누락 엔드포인트는 `/api/cost` 하나다 — `useInfoDashboardYear`가 연도마다 `useCost().fetchCosts`로 전산업무비 전량을 조회하는데 `mockCommonApis`에 그 항목이 없었다(`/api/projects`·`/api/ccodem/`·`/api/banners`는 이미 있다). `mockApi(page, '/api/cost', [])`를 같은 자리에 추가했다. ② **보고서** — 테스트 문제가 아니라 **제품 접근성 회귀**였다. `ApprovalLineSelector`는 표시 라벨(`팀장 지정`)과 aria-label(`팀장 결재자 지정`)을 일부러 다르게 두는데, FE-37 i18n 이관이 둘을 `assignTeamLead` 한 키로 합치면서 미지정 버튼의 접근성 이름이 `팀장 결재자 지정` → `팀장 지정`으로 조용히 바뀌었다(`c5bc074f` 대조로 확인). 화면은 그대로라 눈에 띄지 않았고, 접근성 이름으로 버튼을 찾는 `report-pdf-latest.spec.ts`만 90초 타임아웃으로 실패해 왔다. `assignTeamLeadAria`·`assignDepartmentHeadAria` 키를 되살리고 `ApprovalLineSelector.test.ts`로 계약을 고정했다. 재실행 시 남는 실패가 있으면 그때 다시 판다. |
| FE-48 | 🟢 Low | 테스트 | 알림 헤더 E2E가 부하에 따라 간헐 실패 | `notifications.spec.ts`의 4건 중 1~3건이 실행마다 다르게 실패한다 — 재시도에서 통과하는 실행도 있고 재시도까지 실패하는 실행도 있다(2026-08-19 실측: 전체 실행 시 3건 실패 → 단독 실행 시 1건 → 4개 spec 묶음에서는 flaky 1건). 실패 지점이 `page.reload()` 직후 `getByRole('button', { name: '알림' })` 대기와 드롭다운 열림 대기로, `playwright.config.ts` 주석이 설명하는 Nuxt dev 지연 컴파일 비용에 걸리는 부류다. 고정 timeout을 늘리기보다 뱃지·드롭다운이 데이터 도착 후 안정화되는 시점을 기다리도록 단언을 바꾸는 편이 낫다. **함께 관찰(2026-08-22)**: 단위 테스트에도 같은 성격의 부하 의존 flake가 있다 — `tests/unit/pages/requestFormMigrationPageBoundary.test.ts`의 `누른 부서의 파일만 선택해 다이얼로그에 전달한다`가 `npm test` 전체 실행에서 실패하고 단독 실행(2.6초)에서는 통과했으며, 곧바로 이어서 돌린 전체 실행에서는 다시 통과했다. 이 케이스만 유독 느려(단독에서도 2.5초 이상) 병렬 부하에서 대기 한도에 걸리는 것으로 보인다. E2E와 같은 처방(고정 대기 대신 안정화 시점 단언)이 맞는지 함께 본다. |
| FE-54 | 🟢 Low | 설계 | `WasLogTable`이 설계 §6.1의 가상 스크롤이 아니다 | 평범한 keyed `v-for`라 28rem 뷰포트에 최대 2000행(약 12k DOM 노드)을 그린다. Vue의 keyed 패칭 덕에 3초 재렌더 비용은 감당되나 승인된 설계와 다르고, 2000행 상한이 실제로 무는 유일한 지점이다. 도입하려면 PrimeVue `VirtualScroller`(또는 DataTable `scrollable` + `virtualScrollerOptions`)로 바꾸고, 펼침 행(예외 스택)의 가변 높이를 어떻게 다룰지 함께 정해야 한다 — 고정 `itemSize`가 전제인 구현이라 이 화면의 가변 높이 행과 충돌한다. **2026-08-22 갱신**: 같은 항목의 ②(설계 §6.2 "위로 스크롤하면 자동으로 일시정지 상태가 된다" 미구현)는 해소했다 — 위로 스크롤하면 `paused`를 세우고 하단 복귀 시 자동 해제하되, 사용자가 도구모음에서 직접 누른 일시정지는 자동 해제 대상에서 뺐다. |
| FE-56 | 🟡 Medium | 계약 | `useApprovalLock`이 코드값이 아니라 한글 표시문자열로 잠금을 판정한다 | 신규 `app/composables/useApprovalLock.ts`가 `const APPROVAL_COMPLETED_LABEL = '결재완료'`를 두고 `apfSts === APPROVAL_COMPLETED_LABEL && !isAdmin()`으로 판정한다. 같은 파일 헤더가 "서버는 결재완료(02)를 쓴다"고 스스로 밝히고 있고 `app/types/api.d.ts`에 `apfStsC` 코드 필드가 실재하므로, 루트 CLAUDE §6·프론트 CLAUDE §6의 "업무 분기·저장에는 번역명이 아니라 코드값" 규칙에 어긋난다. FE-37로 사이트 전체가 i18n으로 넘어간 뒤라 표시 문구가 바뀌거나 영어 로케일에서 조회하면 **비교가 실패해 잠금이 조용히 풀린다**(서버가 최종 차단하므로 데이터 손상은 아니고 UX·오해 문제다). 호출부는 `TerminalFormDialog`·`useCostEditingState`·`info/cost/[id]`·`info/cost/terminal/[id]`·`info/projects/[id]` 5곳으로 모두 `apfSts` 표시값을 넘긴다. 조치는 `apfStsC === '02'` 기반으로 바꾸고, 코드 필드가 없는 DTO는 백엔드에 노출을 요청하는 것이다. 2026-08-23 REVIEW. |
| FE-57 | 🟢 Low | 오류 | `/info` 홈의 부가 조회 실패가 "없음"과 구분되지 않는다 | 두 곳이다. ① `useInfoHomeFeed`의 첨부 일괄조회가 `catch { return {} }`로 끝나 호출부의 `attachmentsByPost[…] ?? []`를 거쳐 `BoardAttachmentActions`의 `v-if="files.length > 0"`을 꺼버린다 — 첨부가 있는 공지·일정이 **첨부 없음처럼** 보이고 `noticeState`는 `READY`를 유지하며 `$apiFetch`라 전역 토스트도 없다. ② `pages/info/index.vue`의 경상예산 상세 조회가 실패하면 `ordinaryBudgetDetails`를 `[]`로 두고 `uniqueDisplayValues([])`가 `'-'`를 돌려줘 도입시기·지급주기 카드가 전부 `-`로 보인다 — 미등록과 구분되지 않는다. 홈 화면 부가 정보라 심각도는 낮으나 조회 실패를 성공처럼 보이게 하는 패턴이라 프론트 CLAUDE §2의 재조회 실패 표면화 원칙과 어긋난다. 조치는 실패 플래그를 상태로 올려 인라인 경고나 재시도 배너로 노출하는 것이다. 2026-08-23 REVIEW. |

_FE-37 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-18 FE-37 사이트 전체 고정 문구 i18n 이관 기록을 참조합니다. 2026-07-26 잔여과제 통합 조치의 완료·폐기 이력은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다. FE-21과 FE-30①·FE-28②는 같은 문서의 2026-08-06 잔여과제 저비용 배치 1 기록을, FE-36은 2026-08-09 잔여과제 일괄 조치 기록을, FE-38은 2026-08-17 DDL 불필요 잔여과제 배치 기록을, FE-41·FE-42는 같은 날 배치 4 기록을, FE-40은 배치 5 기록을, FE-39·FE-43은 배치 6 기록을 참조합니다. FE-44(다국어 관리 조회 실패 표면화)는 2026-08-18 다국어 관리·변경로그 구현 배치에서 처리했습니다(`docs/superpowers/plans/2026-08-18-translation-admin-and-change-log.md` 및 이 배치의 커밋 참조). FE-45(다국어 관리 편집 진입 키보드 접근성)는 전제가 거짓으로 밝혀져 과제에서 제외합니다 — PrimeVue 4.5.5는 Enter/Space 키 활성화도 `onEnterKey → onRowClick` 경로로 `row-click`에 합류시켜 애초부터 키보드로 도달할 수 있었습니다._

## ⚙️ 백엔드

| ID    |     우선순위     | 유형  | 과제                                  | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | :----------: | --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BE-24 | 🏛️ External | DB  | 집행 문서 활성 버전 UNIQUE 인덱스의 dev/prod 적용 | 코드·마이그레이션·검증은 2026-08-09에 완료했다([`TASK_DONE.md`](TASK_DONE.md) 2026-08-09 잔여과제 일괄 조치). 남은 것은 **DBA의 dev/prod 적용 하나뿐**이다. 적용 대상은 `it_database/migrations/V20260809_001__AddExecutionDocumentActiveVersionUniqueIndex.sql`(세 테이블에 `IX_TPRMPP_{BDELIM,BCONTM,BPAYMM}_03` 함수 기반 UNIQUE 인덱스), 절차·사전점검·롤백은 `it_database/docs/operations/2026-08-09-be24-active-version-index-handover.md`를 따른다. **사전 점검이 게이트다**: 활성행이 둘 이상인 문서가 있으면 `ORA-01452`로 실패하므로 인계 노트 §2의 중복 조회를 dev/prod에서 먼저 돌려 3건 모두 0인지 확인한다(로컬은 0 확인). 0이 아니면 어느 버전을 남길지 업무 담당자 확인이 선행되어야 하므로 적용하지 않는다. |
| BE-51 | 🟢 Low | DB | `TPRMPP_CFILEM` 보조 인덱스 — **보류** | **2026-08-22 결정: 보류(컬럼 개편 검토 필요).** 인덱스 단독으로 진행하지 않고 컬럼 폭 개편과 함께 다룬다. **효익 실측(로컬)**: 조회 경로는 실재한다 — `FileService.getFiles`·`getFilesBatch`·`deleteFilesByOrc`·`BannerService`(`/info` 진입마다)·`BoardPostFileCacheService.count` 6개 쿼리 형태. 다만 현재 규모에서 병목이 아니다 — 1,527행에 `TABLE ACCESS FULL` **cost 27**(배너 정렬 포함 28). **선택지 판정**: ② 선행 컬럼 단독은 폐기 — `PK_COL_NM`의 서로 다른 값이 **2개뿐**(편성요청서반입 1,523행 / 배너 4행)이라 1키당 평균 764행이다. ① 컬럼 폭 축소가 유일하게 합리적 — 실 데이터 최대 길이가 `PK_COL_NM` **21바이트**, `PK_CONE` **17바이트**인데 선언은 각각 `VARCHAR2(4000)`다(원안 복합 인덱스가 `ORA-01450`으로 실패하는 이유: 선언 키 8000바이트 > 상한 6397). 각 `VARCHAR2(100 CHAR)`면 선언 키 600바이트로 충분하다. 위 수치는 로컬 개발 데이터이므로 운영 실측이 선행돼야 하며, 재개 시 BE-64(통계 수집)를 먼저 본다. |
| BE-52 | 🟢 Low | 조회 | `RequestFormFileReadAuthorizer.canRead`의 부모당 1+M 조회 | 신청서번호당 `findByApfDcmNo` 1회 + 매핑당 원장 `findById` 1회를 실행한다. 단건·단일 부모는 요청 범위 판정 캐시로 충분하나, `getFilesBatch`가 `편성요청서반입` 종류를 다부모로 부르는 화면이 생기면 부모 수 × (1+M) 쿼리가 된다. 그때 Cappla↔원장 조인 프로젝션(신청서번호→주관부서코드)으로 부모당 1쿼리로 줄이거나 batch 판정 인터페이스를 검토한다. 2026-08-19 REVIEW. |
| BE-57 | 🟠 High | 검증 | WAS 로그 피어 팜아웃의 실환경 미검증 | 두 인스턴스가 실제로 `/internal/was-logs/**`로 통신하는 경로를 밟은 테스트가 없다. 단위·슬라이스 테스트가 피어 실패 표면화·토큰 401·비밀값 미설정 시 빈 미등록을 각각 덮지만, 두 프로세스를 띄워 인스턴스 선택·레벨 변경·다운로드가 원격 인스턴스에 도달하는지는 확인되지 않았다(공유 로컬 스키마 때문에 이번 작업에서 기동을 보류). 배포 전 서로 다른 포트·`SERVER_INSTANCE_ID`·동일 `WAS_LOG_INTERNAL_SECRET`으로 두 번 기동해 계획 문서 Task 10 Step 3의 네 항목을 확인한다. |
| BE-58 | 🏛️ External | 운영 | `/internal/was-logs/**` 망 제한과 피어 설정 주입 | 이 경로는 `SecurityConfig`에서 `permitAll`이고 공유 비밀 헤더가 유일한 관문이다(비밀값이 비면 컨트롤러 자체가 등록되지 않아 404). 운영 적용 시 ① 두 인스턴스에 **같은** `WAS_LOG_INTERNAL_SECRET` 주입 ② `WAS_LOG_PEER_SVR1`·`WAS_LOG_PEER_SVR2`에 내부 base URL 주입 ③ 방화벽에서 이 경로를 사내 서버 대역으로 제한 ④ 각 서버 `SERVER_INSTANCE_ID`가 서로 다른지 확인이 필요하다. 비밀값에 작은따옴표를 넣으면 SpEL 조건식이 깨져 기동이 실패한다. 피어 URL이 평문 HTTP면 공유 비밀이 매 폴링마다 사내망을 평문으로 오간다 — HTTPS 권장. |
| BE-64 | 🟠 High | DB | 옵티마이저 통계 수집 여부 확인과 적용 | **로컬 실측(2026-08-22)**: `ITPOWN` 스키마 **93개 테이블 전부 `ALL_TABLES.LAST_ANALYZED`가 NULL**이다 — 통계가 한 번도 수집되지 않았다. 통계가 없으면 옵티마이저가 행수·선택도를 추정으로 판단하므로, 인덱스를 만들어도 타지 않거나 잘못된 조인 순서를 고를 수 있다. BE-51 실측에서 함께 드러났다. **다만 로컬 한정 현상일 수 있다** — 로컬 스키마는 `impdp` **DATA_ONLY**로 만들었고(`docs/`의 임포트 기록 참조) DATA_ONLY 임포트는 통계를 가져오지 않는다. 따라서 **먼저 할 일은 dev/prod의 실제 통계 상태 확인**이다(같은 `ALL_TABLES.LAST_ANALYZED` 조회 한 줄). 운영에 통계가 정상 수집 중이면 이 항목은 로컬 편의 문제로 축소되고, 운영도 비어 있으면 조회 성능 전반에 걸리는 문제가 된다. 조치는 `DBMS_STATS.GATHER_SCHEMA_STATS('ITPOWN')`이며 **DDL이 아니라** 통계 수집이라 스키마를 바꾸지 않는다. 다만 운영 적용은 부하가 있으므로 수집 시점·자동 수집 작업(`auto optimizer stats collection`) 활성 여부와 함께 DBA와 협의한다. 로컬은 개발자가 직접 수집해도 되며, 그 경우 실행계획 실측(BE-51 등)의 신뢰도가 올라간다. |
| BE-65 | 🟠 High | DB | 운영 레이아웃 재구축이 남긴 BYTE 시맨틱 컬럼 | `V20260820_008`이 재생성한 `TPRMPP_BCOSTM`·`BCOSTL.CTT_OPP_NM VARCHAR2(100)`과 `BTERMM`·`BTERML.RMK VARCHAR2(300)`이 **BYTE 시맨틱**이다 — 바로 옆 `CTT_NM VARCHAR2(100 CHAR)`·`NSF_USG_CONE VARCHAR2(200 CHAR)`와 비대칭이고 `ITPOWN_DDL_live.sql`도 동일하다. 엔티티는 `@Column(length=100/300)`으로 **글자 수**를 전제하고 `CostDto`에 `@Size` 가드가 없어, 계약상대처명 34자·비고 101자(한글, AL32UTF8 3바이트)에서 `ORA-12899`로 저장이 실패한다. 같은 배치의 `V20260820_011`이 `FL_NM`·`APG_FL_PTH`에 대해 정확히 이 결함을 고쳤는데 함께 재생성된 이 컬럼들은 빠졌다. 조치는 011과 같은 `MODIFY (… VARCHAR2(n CHAR))` 후속 마이그레이션이며, 후보는 `BCOSTM/BCOSTL.CTT_OPP_NM`, `BTERMM/BTERML.RMK`, `CFILEM.FL_PYS_NM(120)·FL_KPN_PTH(255)`, `CMENUM/CMENUL.IMK_NM(100)`이다. 2026-08-23 REVIEW. |
| BE-66 | 🟡 Medium | 이관 | 마이그레이션 재번호화 — 중간 상태를 적용한 환경의 복구 절차 | 2026-08-20에 적용 대상 스크립트가 세 번 개번됐다: `V20260820_001__SeedWasLogAdminMenu` → `_002`(커밋 `ac71cd3`) → `_005`(커밋 `ba62fa5`), `V20260820_003__AddCommonFileRelativePath` → `_004`(커밋 `e1d7828`, 사유 "concurrent migration version conflict"). `it_database/CLAUDE.md` §2의 "적용·공유된 마이그레이션은 재번호화하지 않는다"에 어긋난다. **이 PC의 로컬 DB는 무사하다** — `ITPOWN.FLYWAY_SCHEMA_HISTORY` 실측 결과 20260820.001~012가 모두 최종 파일명 그대로 `success=1`이라 중간 상태를 적용한 흔적이 없다. 위험은 개번 사이에 pull 후 `local-*` 프로파일로 기동한 **다른 개발자 환경**이다 — 그 환경의 이력에는 `20260820.002 = SeedWasLogAdminMenu`가 남아 지금의 `CreateMfaTransactionTables`와 어긋나므로 Flyway validate가 기동을 막거나, 새 내용(배너 권한 매핑·일정 게시판 코드)이 영영 적용되지 않는다. 조치는 ① dev/prod 미적용 확인 ② 중간 상태를 당긴 개발자용 복구 절차를 `it_database/docs/operations/`에 기록 ③ 병행 작업 시 버전 예약 규칙 명문화다. 2026-08-23 REVIEW. |
| BE-67 | 🟡 Medium | 계약 | 스트리밍 ZIP 다운로드의 검증·권한 판정이 응답 헤더 확정 이후에 실행된다 | `FileController.downloadBoardAttachmentArchive`·`downloadRequestFormSourceArchive`가 `ResponseEntity.ok()` + `Content-Disposition`을 먼저 확정하고 `StreamingResponseBody` 람다 안에서 `writeArchive`를 호출한다. 그 안의 `validateRequest`·`fileService.getFiles`·`resolveSelection`이 던지는 `CustomGeneralException`은 이미 `200 OK`가 커밋된 뒤라 공통 예외 응답 계약(백엔드 CLAUDE §4)을 타지 못하고, 프론트 `useFiles.downloadBoardAttachmentArchive`는 받은 바이트를 그대로 `saveBlob`한다 — 사용자는 **성공으로 보고 0바이트이거나 절단된 ZIP**을 받는다. `writeZip` 도중 `fileService.downloadFile`이 실패하는 경우도 같다. 같은 델타의 `WasLogController.download`는 스냅샷을 먼저 확보하고 실패를 502로 승격해 이 문제를 피하고 있어 두 다운로드 경로의 규약이 어긋난다. 조치는 권한·검증·대상 목록 확정을 응답 빌드 **이전**으로 옮기고 람다에는 확정 목록의 바이트 전송만 남기는 것이며, 중간 실패 회귀 테스트를 함께 둔다. 2026-08-23 REVIEW. |
| BE-68 | 🟡 Medium | MFA | 공급자 예외 원인이 소실돼 MFA 장애를 추적할 수 없다 | `OnePassClient`가 `catch (Exception ignored)`로 받아 `new IllegalStateException("OnePass MFA 통신 또는 응답 처리에 실패했습니다.")`를 cause 없이 던지고, `MfaService`가 다시 `MfaException(MFA_UNAVAILABLE)`로 재포장한다. `MfaService`에는 `@Slf4j`도 `log.*` 호출도 **한 건도 없고**, `GlobalExceptionHandler`는 `log.warn("MFA 거래 거부: code={}")`로 코드만 남긴다. 결과적으로 connect timeout·SSL·5xx·응답 파싱 실패가 모두 `MFA_UNAVAILABLE` 한 문자열로 수렴해 운영자가 근본 원인을 구분할 수 없다(사용자는 실패 자체는 인지하므로 무음 실패는 아니다). 조치는 `catch (Exception e)`로 받아 cause를 체이닝하고 재포장 지점에 `log.warn(..., exception)`을 남기는 것이다. 2026-08-23 REVIEW. |
| BE-69 | 🟡 Medium | DB | `BcostmL`·`BprojmL`에 `CNCD_RFR_NO`가 없어 감사 스냅샷이 조용히 비어 있다 | `V20260820_008`·`V20260820_009`가 만든 `BCOSTL`·`BPROJL`에는 `CNCD_RFR_NO VARCHAR2(30 CHAR)` 컬럼이 있고 마스터 `Bcostm`·`Bprojm`에도 대응 필드가 있는데, 로그 엔티티 `BcostmL`·`BprojmL`에는 필드가 없다. `AuditLogPersister.copyColumnFields`가 **필드명 일치**로 복사하므로 미매핑 컬럼은 예외 없이 영구 NULL로 남는다 — 관련전산업무비번호·관련프로젝트번호의 변경 이력이 감사 로그로 재구성되지 않는다. 조치는 두 로그 엔티티에 `cncdRfrNo` 필드를 추가하는 것뿐이며 DDL 변경은 필요 없다. 2026-08-23 REVIEW. |
| BE-70 | 🟡 Medium | MFA | `TPRMPP_CMFATM.END_DTM`이 NULL인 행은 조회에도 정리에도 걸리지 않는다 | `V20260820_007`이 운영 정렬 목적으로 `END_DTM`을 포함한 8건의 NOT NULL을 해제하면서 헤더에 "엔티티의 `@Column(nullable=false)`는 그대로 둔다"고 밝혔다. 그런데 `MfaTransactionJpaRepository`의 업무 조회는 전부 `AND e.endDtm > :now`, 정리 배치는 `DELETE … WHERE e.endDtm < :cutoff`다. 따라서 어떤 경로로든 `END_DTM`이 NULL인 행이 생기면 조회되지도, 정리되지도 않아 **영구 잔존**한다. `FLUR_NOT`이 NULL이면 `e.failureCount + 1`이 SQL에서 NULL이 되어 실패 잠금이 발화하지 않는다. 앱이 값을 채우므로 현재 발생 경로는 없지만 DB가 더 이상 보증하지 않는 구간이 열렸다. 조치는 `deleteExpiredBefore`에 `OR e.endDtm IS NULL`을 보강하거나 CHECK 제약을 검토하는 것이다. `V20260820_010`의 CLANGM·CLANGL NULL 완화 3건도 같은 성격이다. 2026-08-23 REVIEW. |
| BE-71 | 🟡 Medium | DB | `TPRMPP_CBLBCM`에 `BLB_ID` 선두 인덱스가 없다 | 신규 경량 목록 프로젝션 `BoardPostRepositoryImpl.searchPostRows`가 `BLB_ID = ?` + `DEL_YN = 'N'`으로 거르고 `ANC_YN desc, NAC_UNQ_ID desc, GRP_SQN_SNO asc`로 정렬한다. 그런데 PK는 `PK_CBLBCM(NAC_NO)` 단일이고 보조 인덱스는 `_01(BBR_C)`·`_02(FST_ENR_USID, DEL_YN)`·`_03(HRK_NAC_NO)`·`_04(NAC_UNQ_ID, GRP_SQN_SNO)`뿐이라 **`BLB_ID` 선두 인덱스가 없다**. 목록 매 페이지가 전체 스캔 + 정렬이고 같은 조건의 count 쿼리로 한 번 더 스캔하므로 게시물 누적에 비례해 악화한다. 후보는 `(BLB_ID, DEL_YN, ANC_YN, NAC_UNQ_ID)`이며, 실행계획 판단이 유효하려면 **BE-64(옵티마이저 통계 미수집)를 먼저 확인**해야 한다. 2026-08-23 REVIEW. |
| BE-72 | 🟢 Low | DB | `MAXVALUE 9999` 시퀀스에 `CYCLE`이 걸려 순환 시 PK 충돌이 된다 | `V20260820_006`이 운영 실측값 정렬로 `SQ_TPRMPP_BPROJM_1`·`BCOSTM_1`·`CBLBCM_1` 등 14개 마스터 채번 시퀀스에 `CYCLE`을 걸었다. 이 시퀀스들은 `MINVALUE 1 MAXVALUE 9999`이고 관리번호는 `PRJ-{bseYy}-{seq:04d}` 형식으로 조립된다. 스크립트 헤더는 "상한 도달 시 `ORA-08004` 채번 실패가 사라진다"고 적었지만 실제로는 **하드 실패가 중복 키 실패로 바뀐 것**이다 — 같은 기준연도에서 이미 쓴 번호를 다시 발급하면 PK 위반이고 자동 채번 경로에는 중복 검사가 없다. 운영 DDL도 이미 `CYCLE`이라 즉시 되돌릴 사안은 아니며, 채번 시 존재 확인 재시도 또는 연도별 리셋 설계를 검토 과제로 둔다. 2026-08-23 REVIEW. |
| BE-73 | 🟢 Low | DB | 로그 엔티티 `@Column(length)` 와 물리 DDL 불일치, 마스터↔로그 시맨틱 비대칭, 백필 술어 불일치 | 세 건을 한 항목으로 묶는다. ① `BprojmL`·`BtermmL`의 `@Column(length=)` 다수가 `V20260820_008`·`_009`의 실제 폭과 다르다(예: `ABUS_NM` 200↔100, `USID` 32↔14, `SVN_DPM_C` 100↔20, `BtermmL.SVN_DPM_C` 3↔20). `ddl-auto=none`이라 런타임 실패는 없으나 마스터↔로그 계약이 갈리고 엔티티 파생 문서가 물리 정의와 어긋나며, `validate` 전환이나 스키마 계약 테스트 확장 시 즉시 깨진다. ② 같은 논리 컬럼이 마스터/로그에서 시맨틱이 갈린다(`BCOSTM.TMN_YN VARCHAR2(1)` vs `BCOSTL.TMN_YN VARCHAR2(1 CHAR)` 등) — 현 저장값에서는 무해하나 재생성이 새로 굳힌 비대칭이라 다음 정렬 작업의 기준이 흔들린다. ③ `V20260822_002` 백필이 `REGEXP_LIKE(…, '[^[:print:]]|[가-힣]')`로 이름 여부를 판정하는데 런타임 `UserNameResolver`는 `[^\p{ASCII}]`를 쓴다 — 한자 이름이나 악센트 라틴은 Java는 이름으로 보고 백필은 건너뛴다. 백필 스크립트가 `IS NULL` 가드로 재실행 안전이라 후속 보정이 쉽다. 2026-08-23 REVIEW. |
| BE-74 | 🟡 Medium | 문서 | `./gradlew javadoc`가 Lombok 생성 타입을 못 찾아 실패 | `RequestFormSourceFileArchiver.java:154`의 `private FileDto.UploadRequest.UploadRequestBuilder request(String)` 반환 타입에서 `cannot find symbol: class UploadRequestBuilder`로 **에러 1건**이 나며 태스크가 실패한다. `compileJava`는 통과하므로 코드 결함이 아니라 javadoc 태스크가 Lombok 애너테이션 처리 결과(`@Builder`가 만드는 `UploadRequestBuilder`)를 보지 못하는 문제다. 조치 후보는 ① javadoc 태스크에 애너테이션 처리 경로를 붙이거나 delombok을 거치도록 `build.gradle`을 고치는 것 ② 해당 메서드 반환 타입을 생성 타입에 의존하지 않게 바꾸는 것(메서드 시그니처 변경이라 업무 코드 수정에 해당). 2026-08-23 TEST.md Task 6 점검에서 확인했고, 소스 수정 금지 원칙에 따라 기록만 한다. `--warning-mode all` 기준 `compileJava` 경고는 0건이다. |

_BE-34 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-06 잔여과제 저비용 배치 1 기록을, BE-35·BE-38·BE-39는 같은 문서의 2026-08-17 DDL 불필요 잔여과제 배치 2 기록을, BE-37·BE-40은 같은 날 배치 3 기록을 참조합니다. BE-41·BE-42·BE-43·BE-44·BE-46은 2026-08-18 다국어 관리·변경로그 구현 배치에서 처리했습니다(`docs/superpowers/plans/2026-08-18-translation-admin-and-change-log.md` 및 이 배치의 커밋 참조). BE-49(다국어 변경로그·메뉴 시드 마이그레이션 유실)는 2026-08-19 당일 복원·적용해 종료했습니다([`TASK_DONE.md`](TASK_DONE.md) 2026-08-19 절 참조)._

## 🔄 수기 엑셀 이관

| ID     |     우선순위     | 유형 | 과제                                   | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | :--------------: | ---- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MIG-01 | 🟡 Medium | 이관 | 부문계획 `사업진행` 상태 기록 위치 — **보류** | **2026-08-22 결정: 보류(전반적인 코드 개편 필요).** 어느 방안을 택해도 `ProjectQueryAssembler`의 대표상태 산출이 바뀌어 여러 화면의 사업 상태 표시가 달라지므로, 상태 체계 개편과 함께 다룬다. 재개 시 필요한 것은 이미 정리돼 있다 — 업무 매핑은 확정(진행(품의)→`71`, 계약 체결 전→`75`, 체결 완료→`79`)이고, 남은 것은 기록 위치다. 실측한 `BPROJA` 관례: `CNCD_RFR_NO=PRJ-yyyy-nnnn`(사업 자신) 행이 상태 `01`(157행), `PLN-yyyy-nnnn`(부문계획 문서) 행이 `11`(27행). 계획 문서 행에 쓰면 의미가 어긋나고, 사업 자신 행에 쓰면 예산편성 요청 상태를 덮는다. 새 키를 두는 방안이 유력하다. 함께 확정할 것 둘: ① `진행(계약)`은 시트에 체결 전·후 구분이 없어 `75`·`79` 중 하나로 고정 ② `취소(연기)`는 `IT_PTL_STS_TC` 26개 코드에 대응값이 없어 코드값 추가(DML) 필요. 집행 실적 4열은 금액이라 컬럼이 필요하며(DDL) 상태 매핑과 분리해 별도로 다룬다. 도달할 수 없는 매핑 상수를 미리 넣지 않는다. |
| MIG-12 |      🟢 Low       | 이관 | 편성요청서 국문·영문 대조표 보강 | `FormLexicon`의 라벨·비목 대조표는 런던지점 제출본 1건에서 뽑았다. 다른 해외점포(시드니·뉴욕·도쿄 등)의 번역이 다를 수 있고, 실제로 런던 파일에도 `Machinery` › `Tape backup software`처럼 대응 비목이 애매한 행이 있다. 해외점포 제출본이 들어오면 미해석(`CODE_UNRESOLVED`)으로 남은 어휘를 수집해 대조표에 추가한다. 어휘 추가는 `FormLexicon` 상수 한 줄이면 된다. |

_MIG-02·MIG-04·MIG-06·MIG-15·MIG-19·MIG-20·MIG-21·MIG-23 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-17 DDL 불필요 잔여과제 배치 기록을, MIG-13·MIG-18은 같은 날 배치 3 기록을, MIG-10·MIG-25는 배치 4 기록을, MIG-22·MIG-24는 배치 5 기록을, MIG-03·MIG-14·MIG-16·MIG-17·MIG-26은 배치 6 기록을 참조합니다._

## 🧹 Clean Code 부채

| ID    | 우선순위 | 유형 | 과제                                | 근거/조건 |
| ----- | :------: | ---- | ----------------------------------- | --------- |
| CQ-30 | 🟠 High | 게이트 | `./gradlew spotlessCheck`가 main에서 red이고 `spotlessApply`는 실행 자체가 중단된다 | 2026-08-23 실측: 워킹트리를 clean으로 되돌린 상태에서 `spotlessCheck`가 **40개 파일** 위반을 보고한다(예: 미변경 파일 `RingBufferAppender.java`). 게다가 `spotlessApply`가 완주하지 못한다 — `src/test/java/com/kdb/it/domain/menu/service/MenuPathPolicyTest.java:L58`에서 google-java-format이 `expected token: 'isPreparing_접두일치만참'; generated isPreparing_접두일치만참 instead` lint 오류로 중단한다. 한글 식별자를 쓴 테스트 메서드명이 원인이며, 이 오류 때문에 포매터를 돌릴 수 없어 최근 델타가 전부 미포맷 상태로 누적됐다(위반 40건의 실질 원인). `./gradlew check`가 main에서 실패하므로 백엔드 품질 게이트 전체가 무력하다. 조치는 ① 해당 테스트 메서드명을 ASCII로 바꾸거나 `suppressLintsFor`로 예외 처리 ② 그 뒤 `spotlessApply`로 누적 위반 일괄 해소다. 참고: 이번 REVIEW가 건드린 12개 파일은 개별 포맷을 맞춰 위반 목록에서 제외돼 있다. 2026-08-23 REVIEW. |
| CQ-31 | 🟢 Low | 정리 | 델타로 참조가 끊긴 고아 export와 i18n 키 | 실측(각 항목 전수 검색으로 운영 소비자 0건 확인): ① `app/utils/directoryDrop.ts`의 `readDroppedEntries` — 유일 소비자였던 `RequestFormFolderPicker.vue`가 삭제됐고 대체 조합 `captureDroppedEntries`+`readDroppedEntryRoots`를 `FileDropzonePicker.vue`가 쓴다. ② `app/utils/common.ts`의 `monthFromYmd8` — 유일 소비자 `pages/info/index.vue`가 델타에서 제거됐고 신규 `infoDashboardProgress.ts`의 `dateMonthIndex`가 대체한다. ③ `i18n/messages/admin.ts`의 `admin.requestForm.progress`·`admin.requestForm.dropActive` ko/en 4개 키 — 삭제된 컴포넌트가 유일 사용처였고 대체 키는 `common.fileDropzone.*`다. ④ `FileKindRegistry.knownKinds()` — 호출자 0건(JavaDoc의 "진단과 테스트가 씁니다"는 이번 REVIEW에서 사실대로 정정했다). ⑤ `app/assets/banner/{2027_budget_banner,2027_budget_banner2,itp_banner}.png` 3종 — 참조 0건이며 배너는 `/api/banners`로 서버에서 받으므로 번들에 남을 이유가 없다. ①②는 테스트만 참조하므로 삭제 시 해당 테스트도 함께 정리한다. 2026-08-23 REVIEW. |
| CQ-32 | 🟢 Low | 정리 | 델타가 만든 중복 로직 — 확장자 매핑·날짜 키·폴더 트리 빌더·파일명 안전화 | ① 확장자→아이콘 매핑의 SoT가 둘이다 — `utils/requestFormSourceTree.ts`의 `filePresentation()`과 `components/common/FileFormatIcon.vue`의 `presentationFromFileName()`이 독립 구현이고 **이미 어긋났다**(hwpx 색상 `text-teal-600` vs `text-blue-600`, csv는 컴포넌트만 excel 처리). 소비자도 두 갈래(마이그레이션 화면은 util 결과를 prop으로 주입, 게시판·에디터는 `file-name`만 넘겨 컴포넌트 내부 표 사용). ② `utils/boardSchedule.ts`의 `toBoardScheduleYmd`가 기존 공통 유틸 `utils/common.ts`의 `toLocalDateKey`와 본문이 동일하다 — 기존 쪽이 `Number.isNaN(getTime())` 가드까지 있어 더 안전하다. ③ 상대경로→폴더 트리 빌더가 `requestFormAnalysisFiles.ts`와 `requestFormSourceTree.ts`에 두 벌 추가됐고 basename 헬퍼도 2벌(`basenameOf`/`safeBasename`)이다 — 노드 타입·집계 필드가 달라 통합 비용은 실측이 필요하다. ④ `composables/useFiles.ts`에 파일명 안전화 로직(`'<>:"/\|?*'` 치환)이 `safeApfMngNo`·`safeNacMngNo`로 그대로 복제됐다. 2026-08-23 REVIEW. |
| CQ-33 | 🟢 Low | 테스트 | `infoDashboard*` 유틸의 TSDoc 누락과 계약을 고정하지 못하는 테스트 | `app/utils/infoDashboardProgress.ts`는 공개 함수 4개·인터페이스 2개에 TSDoc이 전무하고 파일 헤더도 없다 — 같은 델타의 형제 유틸(`boardSchedule.ts`·`costDetailDisplay.ts`)은 헤더 배너와 `@param`/`@returns`를 갖춰 일관성이 깨진다. 특히 `buildInfoDashboardProgressRows`의 제외 조건(경상사업·중복 ID·날짜 파싱 실패·윈도우 밖)은 코드를 읽어야만 알 수 있다. `infoDashboardSummary.ts`의 `getInfoDashboardProgressStage`도 `stsTc` 코드 규칙(9로 끝=완료, 0=예산작성, 2·3=사전협의)의 출처가 어디에도 없다. 더해서 `tests/unit/utils/infoDashboardProgress.test.ts`가 모듈 형태를 손으로 선언하고 `progress.buildInfoDashboardProgressWindow?.(…)`처럼 **옵셔널 호출**로 검증해, export가 사라져도 테스트가 통과한다 — 계약을 고정하지 못한다. 2026-08-23 REVIEW. |

_CQ-02~05·07~14 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-21 Clean Code Wave 0·1·2와 2026-07-25 정리 이관 절을, CQ-06·17·21은 2026-08-04 Wave 3 Wave A 절을, CQ-16은 2026-08-04 Wave 3 Wave B·C 절을, CQ-20은 2026-08-01 Prettier 잔여 해소 절을, CQ-25·26은 2026-08-09 잔여과제 일괄 조치 절을 참조합니다. CQ-27·CQ-28은 2026-08-18 다국어 관리·변경로그 구현 배치에서 처리했습니다(`docs/superpowers/plans/2026-08-18-translation-admin-and-change-log.md` 및 이 배치의 커밋 참조)._

## 📝 Tiptap 변수 입력

> 현재 활성 Tiptap 변수 입력 과제 없음. 2026-07-26 완료·폐기 항목과 알려진 제한은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## 📡 실시간 로그

> 현재 활성 실시간 로그 과제 없음.

## 📬 공통 게시판

> 현재 활성 과제 없음.

_BOARD-1(게시판 다국어 메시지의 `@`가 vue-i18n 링크 문법으로 해석돼 500)은 2026-08-19 TEST.md 실행 후속으로 해소했습니다 — `i18n/messages/board.ts`의 ko·en 5개 키를 `{'@'}` 리터럴 보간으로 이스케이프했고, 회귀 방지로 `tests/unit/i18n/messages.test.ts`에 전체 메시지를 vue-i18n 컴파일러에 통과시키는 테스트를 두었습니다. `board.spec.ts` 9건 전부 통과로 확인했습니다._

## 🗂️ 저장소 위생

> 현재 활성 저장소 위생 과제 없음.

_REPO-01 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-17 업무 판단 반영 배치 6 기록을, REPO-02·REPO-03은 같은 날 DDL 불필요 잔여과제 배치 2 기록을 참조합니다._

_2026-08-17 루트 추적 정리: `.agents/`(로컬 에이전트 하네스 설정 4건)·`TaskNotes/`(개인 Obsidian 볼트 12건)·`.review-cache/`(일회성 리뷰 탐지 캐시 9건)·`.superpowers/`(브레인스토밍 런타임 pid·상태 2건)를 `git rm --cached`로 추적 해제하고 `.gitignore`에 등록했습니다. `.superpowers/`는 규칙이 이미 있었는데 추적이 먼저라 무력화된 경우였습니다. 파일은 모두 워킹트리에 남아 있어 로컬 동작에는 영향이 없습니다. `.agents/`의 FP 자료는 손실이 없습니다 — `workflows/Detailed_FP_Estimation_Workflow.md`(105줄)는 추적 중인 `FP/FP.md`(166줄, CSV 산출·생성기 연동 규칙 추가)의 옛 판본이고, `skills/fp/SKILL.md`의 복잡도 매트릭스·점수표도 `FP/FP.md`에 동일하게 있습니다. 나머지 두 스킬(`hookify-rules`, `plankton-code-quality`)은 Codex용 커뮤니티 도구 참조라 이 프로젝트와 무관합니다._
