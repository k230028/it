# IT Portal 잔여과제

|  우선순위   | 의미           |
| :---------: | -------------- |
| 🔴 Critical | 즉시 조치      |
|   🟠 High   | 조속 조치      |
|  🟡 Medium  | 계획 반영      |
|   🟢 Low    | 선택 개선      |
| 🏛️ External | 외부/운영 의존 |
|   ✅ Done   | 확인 완료      |

## 🔒 보안

| ID     |     우선순위     | 유형 | 과제                                  | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | :--------------: | ---- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-12 |     🟢 Low      | MFA  | OnePass FIDO 사용자 거부 상태 구분    | `trResultConfirm`이 `resultCode=100000`이면서 `trStatus != 1`인 응답을 모두 미결정(UNDECIDED)으로 본다. 연동 규격에 사용자 거부를 뜻하는 `trStatus` 값이 정의되어 있지 않아 거부와 대기를 구분하지 못하며, 거부한 거래도 challenge 만료(90초)까지 미결정으로 남는다. 규격에 거부 상태값이 추가되면 `OnePassClient.confirmFido`의 분기에서 `failure()`로 분리한다.                                                     |
| SEC-13 |     🟢 Low      | MFA  | MFA 거래의 다중 인스턴스 지원         | 로그인 대기·MFA 거래를 애플리케이션 메모리에만 두어 서버 재시작 시 진행 중 거래가 모두 폐기되고, 다중 인스턴스 배포를 지원하지 않는다(설계 §8에서 이번 범위 제외). 필요해지면 `MfaTransactionStore`·`LoginPendingTransactionStore` 인터페이스를 공유 캐시 구현으로 교체한다.                                                                                                                                        |

_SEC-04·SEC-05 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-19 Remediation Phase 1, SEC-06은 같은 문서의 2026-07-19 배포 정합성 후속, SEC-08·SEC-09는 2026-07-25 보안 트랜잭션 무결성 절을 참조합니다._

_SEC-10(지정맥 BioAgent 연동 규격 반영)과 SEC-11(지정맥 해시 검증 도입)은 `mfa.md` 반입 직후 같은 작업에서 해소해 표에 남기지 않습니다. 규격과 구현 대조 결과는 `it_frontend/README.md`의 「추가 인증(MFA) 연결」을 참조합니다._

## 🤝 사전협의

> 현재 활성 사전협의 과제 없음. 2026-07-26 완료·폐기 항목은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## ⚠️ 에러 처리

> 현재 활성 에러 처리 과제 없음.

_ERR-09·ERR-10 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-26 에러 표면화·복구 기록을, ERR-06은 2026-07-19 Remediation Phase 1, ERR-05는 같은 문서의 2026-07-19 배포 정합성 후속 기록을 참조합니다. ERR-11·ERR-12는 같은 문서의 2026-07-29 저장 복구·조회 실패 표면화 기록을, ERR-13은 2026-07-31 ERR-13·FE-15~19 조치 기록을, ERR-14는 2026-08-06 잔여과제 저비용 배치 1 기록을 참조합니다._

## 🎨 프론트엔드

| ID    |   우선순위    | 유형   | 과제                                        | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | :-----------: | ------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FE-37 |  🟡 Medium   | 다국어 | 사이트 전체 고정 문구의 i18n 카탈로그 이관 잔여 | 작업 브랜치는 `it_frontend`의 `codex/sitewide-fixed-copy-i18n`이다. 2026-08-16 기준 `node scripts/check-user-facing-copy.mjs --scope app`이 **1553건**을 보고한다. 완료 도메인은 공통 UI·사업(project)·전산업무비(cost)·예산·전자결재·메뉴 관리(admin/menus)·사업예산 홈·사전협의 문서 5화면이며 감사 0건이다. 잔여는 `components/council` 408, `pages/admin` 317, `pages/info`(plan·council-request) 287, `composables` 154, `pages/board` 62, `components/admin` 47, `components/plan` 42, 기타 236건이다. 카탈로그는 `i18n/messages/`에 도메인 파일로 두고 `i18n/locales/{ko,en}.ts`에 등록한다. 화면 단위로 완결해 커밋한다 — 절반만 이관하면 한 화면에 한글·영문이 섞여 이관 전보다 나쁘다. |
| FE-39 |  🟡 Medium   | 다국어 | Excel·문서 출력 문구의 locale 전환           | 화면 문구와 달리 Excel 시트명·헤더는 이관을 미루고 `scripts/user-facing-copy-allowlist.json`에 사유와 함께 기록해 두었다(13건: 사업목록·단말기상세목록·전산업무비·결재상신목록 시트명, `composables/budget/useBudgetWorkExcel.ts` 헤더 9건). 파일명·시트명·헤더를 locale별로 바꿀지, 원장 대조를 위해 한글로 고정할지 업무 판단이 먼저 필요하다. 방침이 정해지면 allowlist 항목을 지우면서 함께 처리한다.                                                                                                                                                                                                                          |
| FE-40 |  🟡 Medium   | 다국어 | 탭 제목의 다국어 지원 부재                   | `definePageMeta`는 컴파일 타임 평가라 `t()`를 부를 수 없어, 이관하면서 각 화면의 `title`을 제거하고 `/api/menus` 메뉴명에 의존하도록 바꿨다(CLAUDE.md §4의 메뉴 단일출처 규칙과 일치). 그 결과 **메뉴에 없는 라우트는 탭 제목이 경로 마지막 세그먼트로 떨어진다** — `documents/list`·`documents/form`·`documents/[id]`처럼 하위 경로로 여는 화면이 메뉴 엔트리인지 확인하지 못했다. 또 메뉴명 자체가 DB 한글이라 언어를 바꿔도 탭은 한글로 남는다. `composables/useTabs.ts`의 우선순위(`meta.tabTitle` → 메뉴명 → `meta.title`)가 i18n 키를 받도록 확장하는 방안을 검토한다.                                                     |
| FE-41 |    🟢 Low     | 다국어 | 고정 문구 감사기의 `export-copy` 오탐 정리   | `export`된 문자열 상수를 전부 표시 문구로 보므로 코드 식별자가 오탐으로 잡힌다(CSS 선택자·localStorage 키·PrimeIcons 클래스·코드값·locale 태그 등 12건). 현재는 `scripts/user-facing-copy-allowlist.json`에 사유와 함께 기록해 억제하고 있으나 줄 번호 기준이라 파일이 바뀌면 되살아난다. 다만 휴리스틱을 ASCII 전용으로 좁히면 영문 표시 문구(감사기 테스트의 `English title`)를 놓치므로, 오탐을 줄이면서 영문 문구를 계속 잡을 판별 기준을 먼저 정해야 한다. **FE-38에서 확인된 별 갈래**: 감사기는 `label` union 값처럼 **로직 값으로 남겨야 하는 한글**도 표시 문구로 세므로, 표시명을 i18n 키로 분리해도 건수가 줄지 않는다(`app/utils/common.ts` 2건, `app/utils/projectTimeline.ts` 10건이 그 상태). 값·표시명 분리가 끝난 파일을 억제할 수단이 필요하다. |
| FE-42 |    🟢 Low     | 다국어 | 고정 문구 기준선 파일의 분할 규약이 미커밋 상태 | FE-38에서 `app/utils/common.ts`(800줄 상한 초과)를 `app/utils/projectTimeline.ts`로 분리하면서 기준선 리터럴 10건이 새 경로로 **이동**했다. 기준선 규칙은 "새 경로 추가 금지"지만 그 취지는 미번역 문구의 증가 방지이므로, 총량이 그대로인 이동은 원 파일 값을 낮추고 새 경로를 등록해 허용한다는 예외를 `scripts/user-facing-copy-baselines.mjs` 주석에 적어 두었다. **문제는 그 파일이 아직 커밋되지 않은 FE-37 작업의 신규 파일이라 이 조정이 워킹트리에만 있다.** FE-37이 커밋될 때 `'app/utils/common.ts': 2`와 `'app/utils/projectTimeline.ts': 10`이 함께 올라가야 하며, 빠지면 `npm run check:copy`가 실패한다. |

_2026-07-26 잔여과제 통합 조치의 완료·폐기 이력은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다. FE-21과 FE-30①·FE-28②는 같은 문서의 2026-08-06 잔여과제 저비용 배치 1 기록을, FE-36은 2026-08-09 잔여과제 일괄 조치 기록을, FE-38은 2026-08-17 DDL 불필요 잔여과제 배치 기록을 참조합니다._

## ⚙️ 백엔드

| ID    |     우선순위     | 유형  | 과제                                  | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | :----------: | --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BE-36 |  🟡 Medium   | 계약  | `totRqmAmt` 이름과 값의 불일치 | DB 컬럼 `TPRMPP_BPROJM.TOT_RQM_AMT`는 **총 예산**(∑품목 AMT)이고, 응답 필드 `ProjectDto.Response.totRqmAmt`는 **당해예산**(`∑AMT − ∑MPL_AMT`)이다. 2026-06-22 설계문서가 이미 "컬럼명은 총소요금액이나 실제 저장값은 당해예산"이라 적었고, 2026-08-16 컬럼 재추가 때 총 예산은 새 응답 필드 `prjBgAmt`로 분리해 충돌을 피했다. 현재는 `ProjectResponseMapper.fromEntity`가 엔티티 값을 동명 응답 필드로 흘리지 못하는 구조라 혼선이 차단되어 있으나(최종 리뷰가 세 저장소 전 소비처를 추적해 확인), 이름 자체를 정리하려면 `totRqmAmt` 소비처(예산목록·상세·PDF·전역검색 등 10곳 이상) 전수 조사가 필요하다. |
| BE-40 |    🟢 Low     | 테스트 | 프로젝션 계약 단정이 합성 메서드에 취약 | `*ProjectionIt`의 읽기 계약 단정 20여 곳이 `getDeclaredMethods()` 결과를 그대로 센다(`.hasSize(7)` 또는 이름 목록 대조). JaCoCo 에이전트는 **본문 있는 메서드를 가진 인터페이스**에 합성 `$jacocoInit`을 넣으므로, 어느 프로젝션 인터페이스에든 `default` 메서드가 추가되면 그 단정이 **커버리지를 켠 실행에서만** 깨진다. 실제로 `AdminUserView.getBbrNm()`이 그 경우였고(MIG-13), 실패 메시지가 데이터 문제처럼 보여 진단을 헤매게 만들었다. 지금 `default`를 가진 프로젝션은 그 하나뿐이라 나머지는 통과하지만 함정은 남아 있다. `UserReadProjectionIt.declaredMethodNames`처럼 합성 메서드를 걸러내는 헬퍼를 공용 테스트 지원(`com.kdb.it.support`)으로 올리고 나머지 단정을 그것으로 바꾼다. |
| BE-24 | 🏛️ External | DB  | 집행 문서 활성 버전 UNIQUE 인덱스의 dev/prod 적용 | 코드·마이그레이션·검증은 2026-08-09에 완료했다([`TASK_DONE.md`](TASK_DONE.md) 2026-08-09 잔여과제 일괄 조치). 남은 것은 **DBA의 dev/prod 적용 하나뿐**이다. 적용 대상은 `it_database/migrations/V20260809_001__AddExecutionDocumentActiveVersionUniqueIndex.sql`(세 테이블에 `IX_TPRMPP_{BDELIM,BCONTM,BPAYMM}_03` 함수 기반 UNIQUE 인덱스), 절차·사전점검·롤백은 `it_database/docs/operations/2026-08-09-be24-active-version-index-handover.md`를 따른다. **사전 점검이 게이트다**: 활성행이 둘 이상인 문서가 있으면 `ORA-01452`로 실패하므로 인계 노트 §2의 중복 조회를 dev/prod에서 먼저 돌려 3건 모두 0인지 확인한다(로컬은 0 확인). 0이 아니면 어느 버전을 남길지 업무 담당자 확인이 선행되어야 하므로 적용하지 않는다. |

_BE-34 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-06 잔여과제 저비용 배치 1 기록을, BE-35·BE-38·BE-39는 같은 문서의 2026-08-17 DDL 불필요 잔여과제 배치 2 기록을, BE-37은 같은 날 배치 3 기록을 참조합니다._

## 🔄 수기 엑셀 이관

| ID     |     우선순위     | 유형 | 과제                                   | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | :--------------: | ---- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MIG-01 |     🟡 Medium     | 이관 | 부문계획 `사업진행`·집행 실적 열의 원장 반영 | 정보기술부문계획 조정의 `사업진행`(`진행(품의)`·`진행(계약)`·`취소(연기)`)과 집행 실적 4열은 대응하는 원장 코드셋이 없어 `BPLANM.REDT_CONE_INF` 계획 JSON 스냅샷에만 남겼다(`docs/superpowers/specs/2026-08-11-excel-bulk-migration-design.md` §5.4). 이 값들을 조회·집계에 쓰려면 `BPROJA.IT_PTL_STS_TC` 또는 `BPROJM.IT_PTL_RPR_STS_TC`의 유효 코드셋을 먼저 확정하고 계약 테스트를 추가한다. |
| MIG-26 |     🟡 Medium     | 이관 | `/budget/work`가 기존 편성률을 조회하지 않고 기본값으로 덮는다 | MIG-15에서 편성률 입력이 소수를 받도록 했으나, 그 화면은 **기존 편성률을 아예 조회하지 않는다** — `calculateDefaultRates`가 DUP 공통코드 기본값(자본 70·일반관리비 100)을 매번 새로 계산해 표에 채운다(설계 §3.3). 그래서 화면을 열었다가 저장하면 소수든 정수든 이미 걸려 있던 편성률이 기본값으로 덮인다. 이관(`applyItemRates`)이 넣은 비목별 편성률도 같은 방식으로 사라진다. 화면 성격이 "일괄 적용"이라 의도된 동작일 수도 있으므로 **기존값을 불러와 초기값으로 쓸지, 덮어쓰기 전에 경고할지 업무 판단이 먼저**다. 기존값 조회 경로는 이미 있다(`ProjectSummaryResponse.categories[].dupRt`). |
| MIG-03 |      🟢 Low       | 이관 | 위임예산 부점 사업의 담당자 지정       | 위임예산 시트에 담당자 열이 없어 `DelegatedBudgetSheetAdapter`가 업로드 사용자 사번(`ctx.actorEno()`)을 담당자·IT담당자로 그대로 넣는다(설계 §5.5). 부점별 실제 담당자를 지정할 수 있는 입력을 미리보기 화면에 추가할 수 있다.                                                                                                                                                                     |
| MIG-10 |     🟡 Medium     | 이관 | 자본예산 품목 비목 보정을 미리보기에서 고를 수 없음 | `CapitalProjectSheetAdapter`는 `devAmountIoeC`·`hwAmountIoeC`·`swAmountIoeC` 보정 키를 받고 `MigrationValidator.checkCapitalIoeOverrides`가 그 값을 검증하지만, 미리보기 표의 보정 드롭다운 선택지는 **그 셀에 걸린 진단의 `candidates`**에서만 나온다. 보정을 아직 넣지 않은 셀에는 진단이 없어 후보도 없으므로 사용자가 기본 비목(개발비 103·기계장치 101·기타무형 106)을 감리(104)·국외(102/105)·SW라이선스(107)로 바꿀 방법이 화면에 없다. 도달할 수 없는 컬럼 라벨은 `it_frontend/app/composables/migration/columns.ts`에서 제거해 두었다. 해소하려면 dry-run 응답에 자본 계열 비목 카탈로그를 실어야 하므로 API 계약 변경이 필요하다. |
| MIG-14 |      🟢 Low       | 이관 | 편성요청서 1-1 단위 뒤섞임의 잔여 미탐지 | 폴백 경로(`총 사업금액(전체기간)` 칸에 접미사가 없어 요약표 배수를 적용하는 경우)의 과대 기재는 **조건 ⑥(모호성 가드, `59cc67f9`)** 으로 대부분 닫혔다 — 배수 적용 해석과 원 단위 해석이 둘 다 `DFR ≥ 0`이면 확정 불가로 보고 미적재+경고를 낸다. 임계값을 쓰지 않아 업무 수치 합의가 필요 없고, 적재값은 그대로 배수 적용값이라 조이기만 한다. **남은 구멍은 좁다**: 원 단위로 적힌 총액에 배수가 곱해졌는데 원 단위 해석의 `DFR`이 음수인 경우(= 선언 총액이 `'26년도 합계 + '26년도 이후`보다 작은 경우)는 모호로 판정되지 않아 부푼 값이 적재된다. 다만 그 조건 자체가 **전체기간 총액이 당해 예산보다 작다는 뜻이라 이미 내부 모순인 파일**이다. 또 접미사가 적혀 있는데 그 접미사 자체가 오기인 경우는 원리상 탐지 불가다(접미사가 그 칸의 단일 출처). 실제 제출본에서 이 잔여 경우가 관측되면 폴백 경로에 `총액 / ('26년도 합계 + '26년도 이후)` 비율 상한을 덧붙여 닫을 수 있으나, **정상 다년도 사업의 비율 상한이 업무 판단**이라 값을 먼저 정해야 한다(단위가 섞이면 5자릿수 이상 벌어지고 정상은 한~두 자릿수로 관측). |
| MIG-12 |      🟢 Low       | 이관 | 편성요청서 국문·영문 대조표 보강 | `FormLexicon`의 라벨·비목 대조표는 런던지점 제출본 1건에서 뽑았다. 다른 해외점포(시드니·뉴욕·도쿄 등)의 번역이 다를 수 있고, 실제로 런던 파일에도 `Machinery` › `Tape backup software`처럼 대응 비목이 애매한 행이 있다. 해외점포 제출본이 들어오면 미해석(`CODE_UNRESOLVED`)으로 남은 어휘를 수집해 대조표에 추가한다. 어휘 추가는 `FormLexicon` 상수 한 줄이면 된다. |
| MIG-16 |      🟢 Low       | 이관 | `/admin/migration` 메뉴명이 옛 성격을 유지      | Task 13에서 화면 성격을 데이터 반입에서 편성률 반영으로 바꾸며 탭 제목(`definePageMeta({ tabTitle: '편성률 반영' })`)만 고쳤다. 메뉴명 단일 출처인 `TPRMPP_CMENUM`의 `/admin/migration` 행은 여전히 `데이터 일괄 반입`이라 사이드바·Breadcrumb에는 옛 이름이 남는다. `it_database/migrations`에 메뉴명 갱신 스크립트를 추가하면 해소된다.                                                                          |
| MIG-17 |      🟢 Low       | 이관 | 위임예산 시트는 조정률 열이 없어 편성률 100% 고정 | 위임예산(경상) 시트는 자본예산 시트와 달리 조정 3열이 없어 종합본 금액을 그대로(편성률 100%) 편성한다. 예산담당자가 위임예산에도 하반기 조정을 걸기 시작하면 시트에 조정률 열을 추가하고 이 가정을 재검토해야 한다.                                                                                                                                                                                              |
| MIG-22 |      🟢 Low       | 이관 | `/admin/migration`·`/admin/migration/requests`의 `tabTitle`이 i18n을 안 거침 | `definePageMeta`는 컴파일 타임 매크로라 `t()`를 인자로 못 받아 두 화면 모두 `tabTitle`에 한국어 리터럴을 그대로 둔다(`index.vue`의 `'편성률 반영'`, `requests.vue`의 `'편성요청서 반입'`). 지금은 한국어 단일 로케일이라 무해하지만, 영어 UI를 지원해야 할 때는 매크로 인자 제약을 우회할 방법(예: 라우트 메타에 i18n 키만 저장하고 레이아웃이 `t()`로 해석)을 마련해야 두 값이 로케일을 따라간다.                              |
| MIG-24 |      🟢 Low       | 테스트 | `@decision-clear`·`@decision-clear-all` 페이지 배선이 무커버리지 | `it_frontend/app/pages/admin/migration/index.vue`의 `@decision-clear`·`@decision-clear-all` 배선 두 줄이 어떤 테스트도 밟지 않는다. 컴포넌트 테스트는 이벤트 방출을, 컴포저블 테스트는 `clearDecision`·`clearDecisionsOf` 동작을 각각 고정하지만 둘을 잇는 페이지 배선만 비어 있다. E2E가 백엔드·프론트 두 서버 기동을 요구해 `feat/migration-allocation` 웨이브에서 돌리지 못한 채 남았다. 기존 이관 E2E 시나리오에 "결정을 고른 뒤 지우기 → 드롭다운이 되살아난다" 단계를 추가하면 닫힌다. 배선 자체가 선언적 2줄이라 위험은 낮다. |
| MIG-25 |     🟡 Medium     | 테스트 | 프론트 단위 테스트 3건이 전체 스위트 병렬 실행에서만 간헐 실패 | `cd it_frontend && npm test`(284파일 3306건)를 돌리면 드물게 3건이 타임아웃으로 실패하고, 같은 트리에서 다시 돌리면 전부 통과한다. 격리 실행(`npm test -- <파일>`)에서는 항상 통과한다. `feat/migration-allocation` 작업 중 여러 구현자가 `tests/unit/composables/useRefreshGuard/onactivated-refresh-guard.test.ts`와 `tests/unit/components/board/refresh-banner-visibility.test.ts`를 이름으로 지목했고, 브랜치 병합 검증 때도 한 번 3건이 실패한 뒤 이어진 두 번의 전체 실행이 모두 통과했다(그때는 파일명을 포착하지 못했다). 병합이 fast-forward라 트리가 동일했으므로 코드 변경과 무관한 실행 환경 문제로 판단된다. **병합 검증·CI 도입 시 거짓 실패로 사람을 멈춰 세우므로 원인을 찾아야 한다.** 조사 방향: `vitest` 워커 수·`testTimeout` 조정으로 재현률을 보고, 두 테스트가 공유하는 타이머·`nextTick`·전역 상태(특히 `useRefreshGuard`의 asyncData 키 공유)에 경합이 있는지 확인한다. 재현이 어려우면 `--reporter=verbose --no-file-parallelism`으로 순차 실행 시 사라지는지부터 가른다. |

_MIG-02·MIG-04·MIG-06·MIG-15·MIG-19·MIG-20·MIG-21·MIG-23 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-17 DDL 불필요 잔여과제 배치 기록을, MIG-13·MIG-18은 같은 날 배치 3 기록을 참조합니다._

## 🧹 Clean Code 부채

> 현재 활성 Clean Code 부채 없음.


_CQ-02~05·07~14 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-07-21 Clean Code Wave 0·1·2와 2026-07-25 정리 이관 절을, CQ-06·17·21은 2026-08-04 Wave 3 Wave A 절을, CQ-16은 2026-08-04 Wave 3 Wave B·C 절을, CQ-20은 2026-08-01 Prettier 잔여 해소 절을, CQ-25·26은 2026-08-09 잔여과제 일괄 조치 절을 참조합니다._

## 📝 Tiptap 변수 입력

> 현재 활성 Tiptap 변수 입력 과제 없음. 2026-07-26 완료·폐기 항목과 알려진 제한은 [`TASK_DONE.md`](TASK_DONE.md)를 참조합니다.

## 📡 실시간 로그

> 현재 활성 실시간 로그 과제 없음.

## 📬 공통 게시판

> 현재 활성 과제 없음.

## 🗂️ 저장소 위생

| ID      |   우선순위   | 유형 | 과제                                       | 근거/조건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------- | :----------: | ---- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| REPO-01 |   🟢 Low    | 설정 | `.vscode/` 설정의 머신 종속 절대경로       | `.vscode/settings.json`이 `java.import.gradle.home: C:/gradle/gradle-9.2.1`과 `java.jdt.ls.vmargs`의 `-javaagent:C:/it/it_backend/lib/lombok.jar`를 절대경로로 박아 두어, 저장소를 다른 경로나 다른 PC에 클론하면 Java 확장 임포트가 실패한다. 두 파일은 **의도적으로 추적한다** — 주석에 중복 빈 등록(`BeanDefinitionOverrideException`)·워크트리 프로젝트명 중복 같은 프로젝트 공통 함정의 원인과 회피책이 기록되어 있어 공유 가치가 크다. 개발자가 늘어나면 머신 종속 값만 `.vscode/settings.local.json` 또는 환경변수로 분리하고, 주석이 담은 함정 설명은 `README.md`로 옮긴다. |

_REPO-02·REPO-03 완료 근거는 [`TASK_DONE.md`](TASK_DONE.md)의 2026-08-17 DDL 불필요 잔여과제 배치 2 기록을 참조합니다._

_2026-08-17 루트 추적 정리: `.agents/`(로컬 에이전트 하네스 설정 4건)·`TaskNotes/`(개인 Obsidian 볼트 12건)·`.review-cache/`(일회성 리뷰 탐지 캐시 9건)·`.superpowers/`(브레인스토밍 런타임 pid·상태 2건)를 `git rm --cached`로 추적 해제하고 `.gitignore`에 등록했습니다. `.superpowers/`는 규칙이 이미 있었는데 추적이 먼저라 무력화된 경우였습니다. 파일은 모두 워킹트리에 남아 있어 로컬 동작에는 영향이 없습니다. `.agents/`의 FP 자료는 손실이 없습니다 — `workflows/Detailed_FP_Estimation_Workflow.md`(105줄)는 추적 중인 `FP/FP.md`(166줄, CSV 산출·생성기 연동 규칙 추가)의 옛 판본이고, `skills/fp/SKILL.md`의 복잡도 매트릭스·점수표도 `FP/FP.md`에 동일하게 있습니다. 나머지 두 스킬(`hookify-rules`, `plankton-code-quality`)은 Codex용 커뮤니티 도구 참조라 이 프로젝트와 무관합니다._
